# -*- coding: utf-8 -*-
"""飆客逐則自問自答：1709 主文＋樓中樓＋兩個社團。

問「他怎麼判斷的」時走判斷鏈，不准再念稿。社團只內化規則，不貼原文、不進 overlay。
點位對官方加權／台指期日 K。夜盤 15 分用期交所成交聚柱，有柱只報高低，不發明段。不是買訊。
"""
from __future__ import annotations

import gzip
import json
import os
import re
import sqlite3
from functools import lru_cache
from typing import Any, Dict, List, Optional, Sequence, Tuple

from tg_layout import html_escape

_DIR = os.path.join(os.path.dirname(os.path.abspath(__file__)), "docs", "expert_notes", "飆客")
WHY_GZ = os.path.join(_DIR, "why_index.json.gz")

_NAME_SID = {
    "台光電": "2383",
    "聯亞": "3081",
    "富喬": "1815",
    "奇鋐": "3017",
    "健策": "3653",
    "建策": "3653",
    "鑑測": "3653",
    "漢唐": "2404",
    "聖暉": "5536",
    "帆宣": "6196",
    "亞翔": "6139",
    "台積電": "2330",
    "台達電": "2308",
    "旺矽": "6223",
    "鴻勁": "7769",
    "穎崴": "6515",
    "穎葳": "6515",
    "金像電": "2368",
    "金居": "8358",
    "南亞科": "2408",
    "南亞": "1303",
    "台塑化": "6505",
    "台塑": "1301",
    "中光電": "5371",
    "雷虎": "8033",
    "事欣科": "4916",
    "長榮航太": "2645",
    "華邦電": "2344",
    "群聯": "8299",
    "廣達": "2382",
    "華碩": "2357",
    "技嘉": "2376",
    "台通": "8011",
    "頎邦": "6147",
    "勤誠": "8210",
    "致茂": "3030",
    "聯發科": "2454",
    "創意": "3443",
    "光聖": "6442",
    "上詮": "3363",
    "波若威": "3163",
    "全新": "2455",
    "鴻海": "2317",
    "樺漢": "6414",
    "智原": "3035",
    "欣興": "3037",
    "景碩": "3189",
    "南電": "8046",
    "台燿": "6274",
    "川湖": "2059",
    "國巨": "2327",
    "大立光": "3008",
    "原相": "3227",
    "萬潤": "6187",
    "汎銓": "6830",
    "泛銓": "6830",
    "精測": "6510",
    "雍智": "6683",
    "穩懋": "3105",
    "IET-KY": "4971",
    "IET": "4971",
    "ABF": "8046",
    "萬海": "2615",
    "新興航運": "2605",
    "新興": "2605",
    "裕民": "2606",
    "緯創": "3231",
    "緯穎": "6669",
    "光環": "3234",
}
_NAMES = sorted(_NAME_SID.keys(), key=len, reverse=True)
_NAME_RE = re.compile("|".join(re.escape(n) for n in _NAMES))
_WAVE = re.compile(
    r"(細微波|波浪|第[一二三四五1-5]波|第[一二三四五1-5]浪|位階|"
    r"下降軌|下降壓|上升軌|1-4|１-４|C-2|C-3|A-c|B-a|abc|5段|9段|右肩)"
)
_INDUSTRY = re.compile(
    r"(產業趨勢|護城河|龍頭|次族群|長線主流|買跌不買漲|抱到|"
    r"PCB|CCL|散熱|光通|矽光子|記憶體|無塵室|廠務|建築)"
)
_VOLUME = re.compile(r"(量先價行|爆大量|價穩量縮|窒息量|破線洗盤|破線翻)")
_IDX = re.compile(r"(大盤|加權|台指|夜盤|費半|那指|細微波|右肩|觀盤)")
_STOCKISH = re.compile(r"(股票|持股|這檔|次族群|龍頭股)")
_FIFTEEN = re.compile(r"(15\s*分|十五分|細微波.*[59]\s*段|[59]\s*段)")
_SIXTY = re.compile(r"60\s*分|六十分")
_TRACK = re.compile(r"(下降軌|下降壓|上升軌|軌道)")
_RETRACT = re.compile(r"(已經沒了|沒了|失敗|開始做頭|改口|更正)")
_FIFTH = re.compile(r"第五波")
_YEAR = re.compile(r"^(?:19|20)\d{2}$")
_NUM = re.compile(r"(?<![\d.])(\d{2,5}(?:\.\d+)?)(?![\d])")
_ALIEN = re.compile(r"(模糊的精確|安全邊際|淨利息|估值位階|沉澱帶|因果鏈|和碩|仁寶)")
_WHY_ASK = re.compile(
    r"(怎麼來|怎麼判|如何知|為何|為什麼|起頭|哪兩檔|潛力|"
    r"走了?\s*5\s*段|下降軌|位階二|護城河|抱到|46506|47578|45839|48218|"
    r"46746|45415|46250|46767|勤誠|8210|"
            r"建築|富喬|聯亞|奇鋐|健策|第五波|9:30|黑手|C-2|C-3|"
            r"高階測試|兩檔黑馬|雍智|汎銓|泛銓|"
            r"洞燭|先機|很少人提|台塑四|台塑集團|"
            r"貫通|串聯|融會|輪動|怎麼連|台光電|改口|跟漲|開口|那晚|同一晚|"
            r"最近|樓下|那則|那篇|補充|還能|能不能抱|怎麼看|怎麼講|"
            r"在說什麼|現在怎樣|那檔|這檔|晚上|他覺得|會不會|要不要|"
            r"有緣人|輔助判|多重比對|沒講出來|某金融商品|"
            r"四路|對質|現在確認|確認了沒|"
            r"現在波浪位階|哪個位階|以波浪|技術線圖|轉折最準|"
            r"9/16|9月16|九月16|Fed|FED|聯準會|升息|"
            r"測底|短線築底|夜盤不太妙|逃命波|強彈|"
            r"C-5|45398|鏡射|10月中|"
            r"上詮|波若威|裸K|碎形|關鍵K|CPO|FAU|築底|先看量|"
            r"金像電|金居|台燿|1245|ABF三雄|9/17低|不破9/17|"
            r"死水|爆量長紅|下殺取量|量價結構|防守點|"
            r"沒看均線|卷商分點|券商分點|大戶籌碼|"
            r"量假結構|主力意圖)"
)
_TALK = re.compile(
    r"(最近|樓下|那則|那篇|補充|改了|還能|能不能抱|怎麼看|怎麼講|"
    r"在說什麼|現在怎樣|那檔|這檔|晚上|夜盤|今天|昨天|他覺得|"
    r"會不會|要不要|還行|怎麼了|那個|這樣|對啊|然後|抱得住|"
    r"要出場|該出嗎|還能抱|他在講|他說什麼)"
)
_RUNTIME_CARDS: List[Dict[str, Any]] = []


def _px(val: Any) -> str:
    try:
        n = float(val)
    except (TypeError, ValueError):
        return ""
    if abs(n - round(n)) < 1e-6:
        return str(int(round(n)))
    s = f"{n:.2f}".rstrip("0").rstrip(".")
    return s


def _ymd(raw: Any) -> str:
    t = str(raw or "").replace("-", "")[:8]
    if len(t) == 8 and t.isdigit():
        return t
    s = str(raw or "").strip()
    if len(s) == 10 and s[4] == "-" and s[7] == "-":
        return s.replace("-", "")
    return ""


def _dash(ymd: str) -> str:
    t = _ymd(ymd)
    return f"{t[:4]}-{t[4:6]}-{t[6:8]}" if len(t) == 8 else str(ymd or "")


def _clip(text: str, n: int) -> str:
    s = re.sub(r"\s+", " ", str(text or "")).strip()
    return s if len(s) <= n else s[: n - 1] + "…"


# 手寫判斷鏈：問為什麼時先走這裡，不是再貼原文。
_TOPICS: List[Dict[str, Any]] = [
    {
        "id": "wave-now",
        "keys": re.compile(
            r"(現在波浪位階|哪個位階|以波浪來看|升浪還是|哪一浪|"
            r"上昇還是下降|上升還是下降)"
        ),
        "q": "現在大盤波浪位階？",
        "a": (
            "只能照他自己點過的標籤，不准發明 5／9 段。"
            "2023-12 開示到 2024-03 公開文沒寫死浪名，不准補教科書浪。"
            "2024-03-15 台指期 3-3-3-4 調整；03-19 修正末端。"
            "2025-03-04 細微波主跌／估破 22000（3/4 官方還沒破，3/11 微破，3/31 低 20696）。"
            "大一級現在：9/11～9/12 他叫右肩／波浪位階二，過 47578 才維持高有過前高，沒升成大 3。"
            "這一組細微波：9/14 叫本波修正末端、頭肩底頸線沒破；21:50 第五波再一次測底。"
            "9/15 09:02：今天強彈反而提高警惕，小心逃命波 C-2。不是已確認 C-2／C-3。"
            "9/16 09:21：收盤不破45398才是C-5低點確認，還是如果句；不到46767≠一定C-3；頭肩底至少3周才可能主升段。"
            "創意最近幾天連續漲勢才代表不會 C 波下殺到 43500；健策、奇鋐跟著噴才 100% 確認。"
            "7/29 他抄底、7/31 收成 A 波低（官方加權低 39385）。"
            "這顆庫加權／台指期柱若缺 2024-03 起的，會自己去抓再對 19660／21937／24730。"
            "沒柱才不對質。不追高；沒看到他發文升成 C-3 就不要替他數下降浪。不是買訊。"
        ),
    },
    {
        "id": "sep15-c2",
        "keys": re.compile(
            r"(逃命波|今天大盤強彈|小心市場黑手|台達電.{0,12}站回|"
            r"破支撐.{0,8}站回|多頭總司令風向球|"
            r"雍智科|精測.{0,8}洗盤|高階測試二軍|"
            r"漲不動我反而|開牌前.{0,8}C-2|開牌後.{0,6}C-3|"
            r"AI關鍵材料|InP再來就是CCL|下飄旗型強力洗盤|"
            r"穩懋.{0,8}台達電|健策.{0,6}跌停|"
            r"初升段走完|該抽出|健策.{0,8}多頭|明天只能上|奇鋐.{0,8}模糊|"
            r"46767|築底是好事|散熱轉弱|止漲整理|裸K|先看量|"
            r"CPO|FAU|上詮|波若威|碎形|關鍵K|"
            r"成交金額.{0,6}失真|43500非常難|沒破線|跌破平台|籌碼交換)"
        ),
        "q": "今天強彈反而小心逃命波？",
        "a": (
            "2026-09-15 09:02 主文 184601742。引用昨天最差情境 C-1，等 C-2 轉 C-3 再出清。"
            "今天強彈反而提高警惕，小心市場黑手開始做逃命波 C-2。不是已確認 C-2／C-3。"
            "觀察句就是 9/14 21:50 那句：接下來反彈不夠高又急轉下殺才會大不妙。"
            "創意最近幾天連續漲勢，才代表不會 C 波下殺至少到 43500；"
            "健策、奇鋐跟著噴才 100% 確認。這是他講的族群盤面結構，不是發明。"
            "台達電前兩天破線、今天站回支撐：正向但證據還不足。"
            "最近多檔主力先殺破支撐、隔 1～3 天站回，最新就是台達電。"
            "09:23 樓下：高階測試二軍雍智已經整理完成；精測先前完成、現在回測洗盤；一軍旺矽、穎崴應該就是時間問題。"
            "09:45 樓下：穩懋昨天跌破支撐立刻站回，細微觀察近期會比台達電強；今年年底或明年大盤漲完目標就是冊歷史高點（他原文）。"
            "官方穩懋 3105 20260911 低 435 收 437.5；20260914 低 420 收 444 量 13,412，昨天這根低破再收上對得上立刻站回。9/15 盤中未收盤不當官方收。"
            "09:53 樓下：健策竟然跌停，主力真狠，這一檔非常難操作。官方健策 3653 20260914 收 5820；9/15 跌停是盤中說法，未收盤不當官方收。"
            "10:24 樓下：InP（聯亞、全新、IET、穩懋）族群還是非常強；最重要龍頭聯亞他認為在做最典型下飄旗型強力洗盤，特別強調不能保證 100% 看對。"
            "IET＝IET-KY 4971。官方聯亞 3081 20260914 收 2745 低 2685；全新 2455 20260914 收 535 漲 3.88%；IET-KY 4971 20260914 開 531 高 559 低 531 收 531 量 1,378。9/15 盤中未收盤不當官方收。"
            "10:41 樓下：AI 關鍵材料最重要是 InP，再來 CCL；CCL 是前一波漲勢最後倒的，當然要整理比較久。"
            "10:47 樓下：目前大盤漲不動他反而覺得比較好；怕開牌前主力作逃命波 C-2，開牌後變 C-3 下殺。不是已確認 C-2／C-3。"
            "官方加權 20260914 收 45863 低 45398。台達電 2308 20260911 低 1595 收 1620；20260914 開 1560 收 1620。9/15 站回是盤中說法。"
            "14:44 樓中樓：富喬有可能初升段走完、目前進入 2 整理，多頭結構沒破，比南亞科／記憶體強太多。"
            "14:47 樓中樓：Yes，如果真的發生 C-2 轉 C-3，布局股票該抽出還是要抽出。不是已確認 C-2／C-3。"
            "14:52 一層：健策多頭結構已經被破壞要進行整理；奇鋐也走到技術分析模糊地帶；明天只能上不能下，再下去就會拉長整理時間，這對大盤是不好的訊號。"
            "夜思考用五件工具對官方柱：波浪／形態／量價／關鍵K（碎形），不是逐則念稿。"
            "夜盤築底是好事、蓄積過下降壓；周四／周五夜盤反彈至少 46767（不是官方收）。"
            "某商品量價說跌破 43500 非常難（成交金額失真、沒點名）；C-2 轉 C-3 未確認，若發生仍先調節、C-3 末端他估 43500 附近。"
            "沒破線續抱：轉折若不是 C 波下殺，2 天漲 1000 是基本。"
            "散熱轉弱由健策 20260915 開 5735 高 5765 低 5240 收 5310 量 2469 爆大量跌破平台確認；籌碼交換至少 2 周；假跌破過幾天。"
            "奇鋐不看月線，因健策轉折K盤中調 1/2。官方 3017 收 3115。"
            "資金轉創意風險大；止漲整理K 一定調節。官方 3443 收 6035＝當日低，還沒連續漲勢。"
            "CPO／FAU ≠ InP：上詮 3363、波若威 3163、光聖 6442；聯亞才是 InP。合聖沒這列。上詮回測頸線、官方收 648。"
            "PCB 金像電／台燿底部趨勢向上，測前高不保證。官方加權 20260915 收 45511。"
            "不數段。不是買訊。"
        ),
    },
    {
        "id": "sep16-c5",
        "keys": re.compile(
            r"(C-5|45398|3日低點|頭肩底.{0,8}3周|鏡射|10月中|"
            r"不到46767|沒反彈到46767)"
        ),
        "q": "收盤不破45398是C-5低點？",
        "a": (
            "2026-09-16 09:21 主文 184659316。"
            "大盤今天觀察只要收盤不破3日低點45398代表C-5低點確認。還是如果句；盤中未收不當官方。"
            "官方加權 20260914 低 45398.43＝他自己點的3日低。最後官方收是 20260915。"
            "明天短線回升＝1；夜盤反彈不到46767則C-2可能性高。"
            "沒到46767≠一定有C-3。"
            "即使化解C波，頭肩底至少3周（鏡射）才可能主升段；操作到10月中買回測支撐或起漲第一根，不是買訊。"
            "45398≠45839：C-5確認線不是右肩低。三個9/16不准混：Fed仍在等、不准編；奇鋐只能上不能下盤中未收；C-5低點也未收。"
            "C-2轉C-3、43500都還未確認。個股不數5／9。不是買訊。"
        ),
    },
    {
        "id": "sep20-pcb",
        "keys": re.compile(
            r"(184841864|金像電.{0,8}1245|1245.{0,8}金像電|"
            r"不破9/17|9/17低點|ABF三雄|"
            r"富喬.{0,10}勉強維持|金居.{0,8}金像電|"
            r"台光電.{0,8}台燿|台燿.{0,8}台光電|"
            r"底部確認可開始布局|立刻出清持股)"
        ),
        "q": "週日 PCB 五檔他怎麼看？",
        "a": (
            "2026-09-20 21:28 主文 184841864，週日晚。同一串判斷，五件＝波浪／形態／量價／關鍵K／碎形線。"
            "個股不數 5／9。明天＝週一 9/21，盤中未收不當官方收。週日沒官方柱。"
            "1 富喬 1815：多頭結構勉強維持，明天不能再下否則破壞。官方 20260917 低 111 收 112。"
            "2 金居 8358、金像電 2368：明天收紅才確認反彈去測前高；前高附近轉弱K立刻出清。"
            "官方 20260917 金居低 477.5 收 478.5；金像電高 1085 低 950 收 950。"
            "21:32 樓中樓：金像電這波反彈高點一定要過 1245；1245 附近轉弱K（長上影／吞噬黑）先走。"
            "官方金像電 20260901 高 1245＝他點的前波高，不是 9/17 高 1085。"
            "3 台光電 2383、台燿 6274：現在同位階。量縮且不破 9/17 低則底部確認可布局，立刻發動有相當難度；技術面比 ABF 三雄強。"
            "ABF 三雄是族群對照，不是點名南電 8046。"
            "官方 20260917 台光電低 4635 收 4695 量 5813；台燿低 1275 收 1320。"
            "路人樓下不當他的判斷。不是買訊。"
        ),
    },
    {
        "id": "sep20-abf-dead",
        "keys": re.compile(
            r"(ABF目前|一攤死水|爆量長紅|下殺取量|進一步觀察要件)"
        ),
        "q": "ABF 現在多空？",
        "a": (
            "2026-09-20 22:57 一層自回（主文 184841864 樓下）。同一串判斷。"
            "ABF 目前就是一攤死水。要等一根爆量長紅出來，才有進一步觀察要件成立；"
            "否則再下跌出現爆量，就非常有可能最後下殺取量。"
            "ABF 是族群（欣興 3037／南電 8046／景碩 3189），不是點名南電。"
            "多空只認量價結構，不數 5／9、不套波浪。週日沒官方柱。不是買訊。"
        ),
    },
    {
        "id": "sep20-vol-only",
        "keys": re.compile(
            r"(量價結構的重要性|完全沒看均線|防守點位|"
            r"卷商分點|券商分點|大戶籌碼|沒看均線|"
            r"只看量價|量價.{0,12}多空)"
        ),
        "q": "他怎麼只看量價就知道多空？",
        "a": (
            "2026-09-20 23:04 二層樓中樓（主文 184841864 樓下）。同一串判斷。"
            "最重要是解讀他發文裡量價結構的重要性。"
            "他完全沒看均線、技術指標 KD、MACD，還有券商分點、大戶籌碼，"
            "就可以很清楚知道多空、防守點位。"
            "個股不數 5／9。不是買訊。路人樓下不當他的判斷。"
        ),
    },
    {
        "id": "sep23-asic",
        "keys": re.compile(
            r"(184902216|最強主流是ASIC|ASIC.{0,8}創意|"
            r"創意.{0,8}聯發科.{0,12}歷史高點|新F系列|"
            r"F系列名單|健策.{0,16}整理完成|金像電.{0,20}整理完成|"
            r"CPO.{0,8}大立光|執著CPO|2~3成持股|"
            r"不太可能整理超過2個月|全新比聯亞更穩健|"
            r"光通訊正規軍)"
        ),
        "q": "9/23 ASIC 主流他怎麼看？",
        "a": (
            "2026-09-23 08:39 主文 184902216。同一串判斷，五件＝波浪／形態／量價／關鍵K／碎形線。"
            "個股不數 5／9。波浪不准套個股（17:57 自回：不能用波浪理論操作股票）。不是買訊。"
            "形態：最強主流 ASIC；創意、聯發科 5 月見高後整理 4 個月就有效過歷史高，和川湖、大立光長整理或底部起漲過高不同。"
            "ABF、封測已到歷史高附近，一鼓作氣真突破不容易。散熱兩檔龍頭高檔整理很久，再拉一波機率較大。"
            "InP 受 ASIC 影響連續漲勢不容易，他希望看錯；聯亞、全新昨天減碼仍佔最高。"
            "官方 20260923：加權開 47895 高 48342 低 47895 收 48157；"
            "創意 3443 開 8325 高 8540 低 8200 收 8385 量 2107；"
            "聯發科 2454 開 5220 高 5265 低 5140 收 5185 量 6430；"
            "川湖 2059 開 12670 高 12725 低 12320 收 12390 量 350；"
            "大立光 3008 開 6440 高 6470 低 6140 收 6190 量 1689；"
            "聯亞 3081 開 2825 高 2845 低 2585 收 2680 量 4144；"
            "全新 2455 開 541 高 552 低 535 收 552 量 3310。"
            "10:30 一層：健策量價告訴他整理完成，萬元股潛在競爭者。官方 3653 開 6005 高 6410 低 5930 收 6410 量 1525（收在當日高）。"
            "10:32 樓中樓：散熱整理很久沒破支撐，細微量價籌碼沉澱。"
            "11:25 樓中樓：金像電量價整理完成，小心台光電比他說的還快整理完。官方 2368 開 1085 高 1140 低 1030 收 1140 量 14173；台光電 2383 開 4910 高 5110 低 4860 收 5020 量 2450。"
            "金像電 9/23 高 1140 還沒過他 9/20 點的 1245。"
            "12:58 樓中樓：聯亞今天破線又站回支撐且爆量是好事；上詮更弱；專注聯亞、全新，像样行情等到 12 月或明年。官方上詮 3363 開 660 高 665 低 631 收 643。"
            "13:03：全新比聯亞更穩健。13:48：勤誠 8210 沒量＝沒主力要做。官方 8210 開 906 高 912 低 891 收 892 量 1253。"
            "17:29 一層：CCL／InP 整理不太可能超 2 個月；台光電、聯亞要有想投入資金的 2～3 成。不是買訊。"
            "17:34：全新這階段是 InP 風向球，比聯亞還重要。"
            "17:56：CPO 主角是大立光不是上詮，執著 CPO 者換股大立光。18:25：光通訊正規軍自選聯亞、大立光、全新。"
            "18:36：新 F 系列七檔＝台積電、創意、台光電、奇鋐、健策、聯亞、川湖；觀察＝大立光、聯發科、台燿、金像電。"
            "官方台積電 2330 開 2475 高 2505 低 2475 收 2500 量 22817；奇鋐 3017 開 3425 高 3590 低 3410 收 3470 量 2649；"
            "台燿 6274 開 1440 高 1485 低 1420 收 1460（櫃買日 K）。9/17 低台光電 4635、台燿 1275 這根都沒破。"
            "20:13：川湖很久沒看，只知道高檔震盪整理、不知道要整理多久。"
            "路人樓下問奇鋐／雙鴻不當他的判斷。16:48 量假＝量價見另一則。"
        ),
    },
    {
        "id": "sep23-vol-fake",
        "keys": re.compile(
            r"(量假結構|主力意圖|穩定性差|技術指標拿掉|K棒更清楚|"
            r"懂量假|懂量價結構|隱藏的主力)"
        ),
        "q": "量價結構是什麼？",
        "a": (
            "2026-09-23 16:50 一層自回。同一串判斷。"
            "自回打成「量假結構」，正文是量價結構（五件③）。"
            "懂量價結構，久了就自然會把穩定性差的技術指標拿掉，K 棒更清楚，才看得出隱藏的主力意圖。"
            "不是新指標。用官方柱重述他已講過的："
            "爆量長上影＝出貨／轉弱K（金像電 20260831 高 1240 收 1165；9/1 高 1245 收 1225 不是這根）；"
            "爆量收貼當日低＝要整理或還要再跌（協易機 4533 20240415、鴻海 2317 20250113）；"
            "破線 1～3 日站回＝洗盤／假跌破，不准升轉折K。"
            "洞燭落後檔『量起來』若是前兩種，不准當發動。不進海選、不改黃金買點。個股不數 5／9。不是買訊。"
        ),
    },
    {
        "id": "sep24-slow-up",
        "keys": re.compile(
            r"(184931175|緩步上攻|緩步攻堅|九組推升|"
            r"關前整理|下星期二.{0,12}歷史新高|"
            r"健策.{0,8}真突破|聯茂.{0,12}跳空|"
            r"CCL.{0,12}整理結束|47000.{0,12}假跌破|"
            r"滾量上攻.{0,8}健策|今天可特別關注健策|"
            r"四檔.{0,16}2.?3成|台燿.{0,16}前高|全新並沒有這麼快|"
            r"不太可能像聯發科一樣直接噴)"
        ),
        "q": "9/24 緩步上攻他怎麼看？",
        "a": (
            "2026-09-24 08:57 主文 184931175。同一串判斷，五件＝波浪／形態／量價／關鍵K／碎形線。"
            "個股不數 5／9。九組推升脈動是他自己的如果句：要看整理時間／拉回幅度有沒有破壞長線波型才能判斷，目前證據不夠。"
            "不准當已確認九組、不准發明 5／9、不准把波浪套個股。不是買訊。"
            "官方 20260924 加權收 48024.6（證交所；較 9/23 收 48157 小跌）。"
            "個股 9/24 這機 daily_quotes 還沒這列，對質用 20260923："
            "創意 3443 開 8325 高 8540 低 8200 收 8385；聯發科 2454 開 5220 高 5265 低 5140 收 5185；"
            "健策 3653 開 6005 高 6410 低 5930 收 6410；奇鋐 3017 開 3425 高 3590 低 3410 收 3470。"
            "聯茂 6213 本地最後完整日 K 是 20260917 收 495，9/23／9/24 還沒這列。"
            "形態：大盤緩步上攻／緩步攻堅可能性較大；多數整理少數動。早盤先不買、調節，尾盤再考慮介入。"
            "09:28 上漲緩漲、13:43 與 14:09（回路人問台積電中秋後）都是緩步攻堅，不另開大盤判斷。"
            "標示四檔＝創意、聯發科、健策、奇鋐。09:18：四檔趨勢向上、慢慢等。"
            "09:27 健策真突破；10:15 不要一次 all in；10:46／10:48 改口滾量上攻；11:02 不好操作改穩健做奇鋐。"
            "奇鋐關前整理完成、下星期二主升。待驗證，不是保證。"
            "09:54／09:56 聯茂跳空→幾乎 90% CCL 整理結束；支撐區先買台光電、台燿 1/2。"
            "13:35 台燿初步整理完成。14:08：到前高後一段時間整理可能性最大，不太可能像聯發科直接噴，到時再看。待驗證。"
            "08:38 在 9/23 串：全新沒這麼快整理完成；ASIC／散熱回測支撐量縮買點要尾盤才知道。"
            "12:04 南亞科難短時間過前波高，比散熱弱。"
            "11:43 四檔續航，不動資金至少 2～3 成抱到中秋後。不是買訊。"
            "11:52 44000＝波浪沒學通；修正跌到 47000 以下都是最後假跌破——他自己點的。"
            "主文已寫的緩步攻堅／奇鋐關前／動態調整，樓下相同句不當新判斷。路人樓下不當他的判斷。"
        ),
    },
    {
        "id": "wave-eyes",
        "keys": re.compile(
            r"(技術線圖|他看到什麼|看大盤轉折|轉折最準|沒人比|"
            r"用了?(什麼|啥)工具|大盤對比類股)"
        ),
        "q": "他技術線圖到底看到什麼？",
        "a": (
            "不是 KD／MACD／布林／均線。先定大盤：台指期夜盤→15 分細微波 5 或 9、60 分、"
            "下降壓連他自己點過的高（如 48218）；加權日K對質 45839／47578／48218；常先畫台積電兩個低連線。"
            "確認低點他明講波浪沒辦法 100%，要疊台積電量價、費半／那指、夜盤有沒有先過下降壓。"
            "2026-07-31：「要這麼精準抓到 7/29 是 A 波低點，最主要就是憑藉對波浪理論細微波的波型」。"
            "類股看輪動、誰先過前高、龍頭攻或休；個股看量先價行＋相對龍頭位階，不把波浪套個股。"
            "2026-09-15 夜他自己講真正有用＝波浪、形態、量價結構、關鍵K棒（碎形K）；均線／指標／成交金額淘汰或失真。"
            "五件回測：破線當天不篤定（真破約兩成）；量縮站撐後 20 日約七成收正仍要疊大盤不是主跌。"
        ),
    },
    {
        "id": "46506",
        "keys": re.compile(r"(46506|穿越.?465|今晚.*末端|確認.*末端)"),
        "q": "46506 怎麼來？",
        "a": (
            "2026-09-11 17:49 主文：確認短線修正末端，今晚夜盤至少穿越 46506。"
            "官方台指期 20260910 日盤（regular）低剛好 46506，不是加權 9/10 低 46573。"
            "這是前一日台指日盤低當確認水平。9/11 夜盤官方高 46663、收 46576，已過這水平。"
            "同則「15 分細微波走 5 段、下降軌已破壞」是他夜盤 15 分圖上數的；"
            "夜盤 15 分用期交所成交聚官方高低，不數他圖上哪 5 段、不假裝畫過那條軌。"
        ),
    },
    {
        "id": "47578",
        "keys": re.compile(r"(47578|過前波高|下周.*右肩|高有過前高)"),
        "q": "47578 怎麼來？下周過它才維持右肩？",
        "a": (
            "2026-09-12 10:34：下周要過前波高點 47578，才能維持高有過前高做右肩，還在波浪位階二。"
            "官方加權 20260908 高 47578.24。台指期日盤 9/8 高 47593。所以「前波高」＝9/8 這波高，不是亂編。"
            "更大一級前高是加權 20260623 高 48218.87（他 9/10 說下波起漲至少測 48218）。"
            "過 47578＝近波右肩還在；還沒把計數升成大 3。位階不講死。"
        ),
    },
    {
        "id": "sep14-end",
        "keys": re.compile(
            r"(本波指數修正的末端|8/14.{0,4}8/19|Apple股王|光學族群|"
            r"降低2/3|C波下殺C-1|南亞科中線多頭|不要隨便亂殺低|"
            r"重要留言看法分享|頭肩底|43500|創意應該是第一檔|ASIC.{0,8}風向球|"
            r"PCB不要亂動|富喬再度回到支撐|大立光.{0,20}光通訊|嚴格不能算光通訊|"
            r"聯亞的InP)"
        ),
        "q": "9/14 這則指數末端、光學跟聯亞怎麼分？",
        "a": (
            "2026-09-14 09:51 主文 184578674，後來改主文補「重要留言看法分享」。"
            "60 分鐘他說最後還是 1-2-3-4-5 不是 abc。最差 C-1，等 C-2 轉 C-3 再出清，目前言之過早。"
            "9/11 樓下才說若出現 C-2 轉 C-3 會發文——這則還沒確認轉折，只說更可能像 8/14～8/19，已到本波指數修正末端。"
            "改主文：消息面多跌一段，本來 60 分 abc／15 分 5 段已結束，變成 60 分 5 段＝本波修正末端；回測頭肩底頸線，型態沒破。不數段。"
            "記憶體、ABF 不是看壞，整理 1～3 個月跑不掉；同期不如布局相同或更低位階的 PCB 指標股或上游材料。"
            "創意應是第一檔噴出去，ASIC 變成大盤先行風向球、取代旺矽。創意、健策（加上奇鋐更好）噴出去，才確認大盤不會 C 波下殺到 43500。43500 是他原文，不是官方收。"
            "光學族群（Apple 股王）不是光通訊：光學漲勢結束、周線 abc、至少降 2/3，整理 2 個月跑不掉。"
            "光通訊 InP 龍頭聯亞回到次級四浪、中期支撐區，中線多頭未破壞。"
            "奇鋐如同 8/21、8/24，多頭沒破；健策比奇鋐強看型態。"
            "南亞科中線多頭被破壞，記憶體持續整理像 ABF。"
            "PCB 7 月最後轉弱，很多還在底部，不要亂殺低。"
            "樓下：PCB 不要亂動，位階最低，未來漲勢大於等於光通訊。富喬再度回到支撐區。"
            "樓下：大立光光通訊不算關鍵，最重要聯亞 InP；還有聯電占比很小，嚴格不能算光通訊。"
            "盤中發文，不把當日截圖當官方收。路人樓收進討論串、正文不當他的判斷。不是買訊。"
        ),
    },
    {
        "id": "sep14-retest",
        "keys": re.compile(
            r"(感覺夜盤不太妙|再一次測底|短線築底|第五波再一次測底)"
        ),
        "q": "夜盤不太妙？第五波再一次測底？",
        "a": (
            "他習慣把路人的話放進引號，後面才是自己回答。這則引「感覺夜盤不太妙」。"
            "2026-09-14 21:50 他答：沒有不太妙。目前是對第五波再一次測底，不重要；"
            "要看接下來反彈不夠高又急轉下殺，才會大不妙。"
            "今天就是再一次測底（或稱短線築底，等反彈）。"
            "掛在同日 09:51 主文樓下。主文仍說本波修正末端、頭肩底頸線沒破、C-2 轉 C-3 言之過早。"
            "夜盤 15 分用期交所成交聚官方高低，不數他圖上第幾段。不是買訊。"
        ),
    },
    {
        "id": "fed916",
        "keys": re.compile(
            r"(9/16|9月16|九月16|0916|Fed|FED|聯準會|升息|利率決策|等待9)"
        ),
        "q": "9/16 Fed 他在等什麼？",
        "a": (
            "不是看新聞做股票。是他自己點的日曆。"
            "2026-09-04：目前影響股市最大因素是 FED 是否升息；如果升息，川普期中選舉一定慘敗，想想可能嗎？"
            "2026-09-07 樓下：指標龍頭要有效過前波高再拉一波，等 9/16 以後可能性比較大。"
            "2026-09-08：等待 9/16（Fed 9/15～9/16 利率決策）。"
            "如果沒有升息，已在新史新高的個股理應再表態拉一波；到時還在高檔震盪就積極調節、找新標的。"
            "真正下一波主流要下星期才能確認。升息結果這顆庫沒這列不准編；到點用新高檔官方日K回撤。不是買訊。"
        ),
    },
    {
        "id": "46746",
        "keys": re.compile(r"(46746|45415|46250|不走.?9\s*段|7\s*段跌完|前波低.?454)"),
        "q": "46746、45415 怎麼來？穿刺才不走 9 段？",
        "a": (
            "2026-09-02：要穿刺 46746 才不走 9 段；7 段跌完看前波低 45415；夜盤 46250～46300 要有效穿刺。"
            "官方台指期日盤 15 分：9/2 08:45 那根高剛好 46746，收 46189 沒站穩；"
            "8/31 10:30 那根低剛好 45415；8/13 08:45 那根高剛好 46250。"
            "9/2 當夜 15 分官方高 46407 低 45686，沒過 46746。"
            "9/3 夜盤（他 9/4 說睡前已破下降軌）15 分官方高 46559 收 46487。"
            "點位對官方高低，不數他圖上哪 7 段／9 段。不是買訊。"
        ),
    },
    {
        "id": "qincheng60",
        "keys": re.compile(r"(勤誠|8210).{0,12}(背離|60\s*分|出清)|(背離|60\s*分|出清).{0,12}(勤誠|8210)"),
        "q": "勤誠 6/19 60 分背離——庫有沒有當天 60 分？",
        "a": (
            "2025-06-19 他看勤誠 60 分疑似背離、全部出清。規則是連續攻擊才能講量價背離，不是發明公式。"
            "官方 8210 當天日線收 473。Yahoo 60 分當天 5 根，高 479 低 460（11:00 那根高 479）。"
            "60 分當日頂 479 不是日線頂 473；他 7 月又買回。不把這句寫進海選。不是買訊。"
        ),
    },
    {
        "id": "45839",
        "keys": re.compile(r"(45839|9/3.*低|守住.*低|低不破前低)"),
        "q": "45839 怎麼來？",
        "a": (
            "2026-09-11 08:43：未來 2～3 日觀盤重點是 9/3 低 45839 有沒有守。"
            "官方加權 20260903 低 45839.36。有守＝右肩還是高有過前高、低不破前低。"
            "9/11 加權低 45942 收 46185，當日沒破 45839；這不是保證後面不破。"
        ),
    },
    {
        "id": "48218",
        "keys": re.compile(r"(48218|下波起漲|測.?482)"),
        "q": "48218 怎麼來？",
        "a": (
            "2026-09-10 主文：細微波 9/8 起 abc、當天 c 末端；下波起漲目標至少測 48218。"
            "官方加權 20260623 高 48218.87。他 7/23 畫 60 分下降壓也是從這高連下來。"
            "47578 是 9/8 近波高；48218 是更大一級前高。兩檔不要混。"
        ),
    },
    {
        "id": "fifth",
        "keys": re.compile(r"(第五波|起頭|2027.*第五|第五波.*結束|第五波.*起)"),
        "q": "台光電抱到 2027 第五波結束——第五波起頭在哪？沒起頭怎麼結束？",
        "a": (
            "他改口過，不准釘死一個起點。2025-12-03：台積電 9/3 低連 11/24 低（官方 2330 低 1145／1375），"
            "或加權 9/3 低連 11/21 低（官方 11/21 低 26396），不破才談那組第五波。"
            "2025-12-15：1-4 重疊、軌道幾乎破壞，那組末升第五波他判定失敗、開始做頭。"
            "2026-07-02：最樂觀＝第五波兩次擴延，綠上升軌不能破，風向球旺矽。"
            "2026-07-07：這條樂觀第五波沒了，改 A-c，目標破 44454 往 42206。"
            "2026-09-11 23:15「抱到 2027 年大盤第五波結束」是更大一級、產業趨勢還在才講得出口；"
            "那則沒標這級第 1 浪起點。對得上的是：12 月那組與 7 月擴延都被他否決；"
            "7/29 加權低 39385 可視為大 A；現在高檔震盪／右肩／位階二＝還沒把計數升成大 3。"
            "沒起頭就講結束＝產業趨勢期限，不是已經數到第五波推動段。不准編一個起點。"
        ),
    },
    {
        "id": "emc",
        "keys": re.compile(
            r"(台光電.*護城河|護城河.*台光電|抱到.?2027|抱到明年|7\s*月抄底|"
            r"為何.{0,12}篤定|為什麼.{0,12}篤定|買跌不買漲)"
        ),
        "q": "台光電護城河最高、可抱到 2027——怎麼判？",
        "a": (
            "不是把波浪套在 2383。鏈是產業趨勢＋龍頭＋大盤大跌窗口。"
            "2025-11-21：賺錢永遠產業趨勢＋該產業龍頭；技術分析主要逃股災或賺價差。"
            "2026-04-16：台積電、台達電、台光電、旺矽、穎崴、奇鋐＝可抱到明年，要在大盤大跌時介入。"
            "2026-07-06：AI 長線龍頭買跌不買漲；台光電倒了就是 AI 時代結束。"
            "2026-07-22：PCB 是 AI 供應鏈最重要，台光電地位僅次台積電。"
            "2026-07-23：AI 高速 CCL 世界第一。"
            "2026-07-29／30：大盤抄底窗口對上加權 7/29 低 39385；台光電官方日 K 7/29 低 3985 收 4100，7/30 低 3930 收 4315。"
            "2026-07-30：F4 有護城河高毛利。9/11 是這條鏈收斂，不是新發明。不是買訊。"
        ),
    },
    {
        "id": "lianya",
        "keys": re.compile(r"(聯亞|下一個台光電|矽光子最重要|風向球)"),
        "q": "下一個台光電為什麼是聯亞？",
        "a": (
            "不是再找一檔 CCL，是下一個有護城河的產業龍頭位。"
            "2026-09-10 樓下：矽光子最重要一檔就是聯亞；龍頭不見得最會漲，但是風向球。"
            "光通訊跟漲先看聯亞，同他看散熱一定看奇鋐、健策。9/9 聯亞破線洗盤後鎖漲停，他當龍頭確認。"
            "9/11 才把聯亞收成「下一個台光電」。沒寫聯亞目標價就不准編。"
        ),
    },
    {
        "id": "build",
        "keys": re.compile(r"(建築兩檔|漢唐|聖暉|無塵室|廠務|哪兩檔.*建築|建築.*哪兩檔)"),
        "q": "建築兩檔是哪兩檔？",
        "a": (
            "2026-09-11 沒點名代號，不准假裝已點名。"
            "公開文裡他常把漢唐 2404、聖暉 5536 當台積電廠務／無塵室指標"
            "（2025-06 漢唐；2025-08 漢唐、帆宣、聖暉；2025-08-26 指標股是漢唐及聖暉）。"
            "社團 2026-06-16 內化：漢唐其實包含聖暉，當時轉弱先不要碰——規則可內化，原文不上話筒。"
            "9/11 的「建築兩檔」最像這條廠務鏈收成兩檔，但沒寫死漢唐＋聖暉。帆宣也曾並提。沒點名就明講沒點名。"
        ),
    },
    {
        "id": "hi_test",
        "keys": re.compile(
            r"(高階測試|高階檢測|兩檔黑馬|測試黑馬|6683|6830|雍智|汎銓|泛銓)"
        ),
        "q": "高階測試兩檔黑馬是哪兩檔？",
        "a": (
            "最早點名是雍智 6683、汎銓 6830。"
            "公開 2026-02-24：高階半導體檢測很少人提，領頭羊穎崴 6515，泛銓也可以介入做波段"
            "（當天收 236，之後 20 日約 +83%，3/9 高 411）。"
            "雍智是同一條高階測試／探針卡鏈的另一檔最早黑馬，不是去替代穎崴、旺矽。"
            "2026-04-18 樓中樓：汎銓新聞變多＝中短期高點。不是買訊。"
        ),
    },
    {
        "id": "fuqiao",
        "keys": re.compile(r"(富喬|上游材料|1815)"),
        "q": "富喬也很有潛力——如何知道？",
        "a": (
            "不是忽然喊。2026-09-09 樓下：請 AI 分析 PCB 上游材料，答案和他用技術分析一樣，首選富喬。"
            "2026-09-10 主文：PCB 除次族群龍頭 CCL 台光電、高階 PCB 金像電可長抱外，"
            "上游材料關鍵供應商尤其富喬應該是首選。9/11 才說也很有潛力。"
            "2026-09-14 樓下：富喬再度回到支撐區。"
            "判斷＝CCL 龍頭之下的次族群＋技術面與產業材料分析對上。官方 1815 9/9 高 135 低 126 收 126。"
        ),
    },
    {
        "id": "heat",
        "keys": re.compile(r"(奇鋐|健策|第一批創新高|長線主流|散熱.*最強)"),
        "q": "奇鋐、健策為什麼是第一批創新高的長線主流？",
        "a": (
            "方法是次族群第一名、誰先過前高，不是事後看新聞。"
            "2026-09-10：目前長線主流股族群最為強勢＝散熱。7/21 他說過散熱奇鋐已修正完成。"
            "2026-04-16 奇鋐已在可抱到明年的長線龍頭名單。9/12 00:36：因為也是第一批創新高的長線主流股。"
            "官方近高：奇鋐 3017 在 20260907 高 3595；健策 3653 在 20260901 高 6095。"
            "第一批創新高＝這套領頭羊規則。不是買訊。"
        ),
    },
    {
        "id": "five_seg",
        "keys": re.compile(r"(走了?\s*5\s*段|五段|細微波.*5|完整結構)"),
        "q": "夜盤 15 分細微波走 5 段——怎麼看出五段？",
        "a": (
            "方法在 2026-04-07 細微波四步：認識調整型態 → 拆線 → 完整結構出現（5 或 9 段）後消去不符合的"
            " → 升／降軌道破壞才算轉折。工具＝台指期 15＋60＋夜盤連續盤。"
            "9/11 17:49 是他夜盤 15 分圖上數完 5 段。"
            "夜盤 15 分用期交所成交聚官方高低，不能假裝數過這則的段。"
            "同則下降軌破壞＝四步第 4 步的初步止訊號，還要穿越 46506 才談確認末端。"
        ),
    },
    {
        "id": "downtrend",
        "keys": re.compile(r"(下降軌|下降壓|軌道.*破壞|怎麼畫)"),
        "q": "下降軌破壞——軌道在哪、怎麼畫？",
        "a": (
            "軌道＝圖上連點，不是均線。下降壓／下降軌＝同一次級兩個更低的高連起來；"
            "上升軌＝同一次級浪 2 低連浪 4 低。"
            "9/11 17:49：夜盤 15 分下降軌道已破壞＝初步止訊號。22:41：夜盤已破夜盤 60 分下降壓、在回測。"
            "夜盤 15 分用期交所成交聚柱，仍不能假裝畫過圖上那條線。60 分下降壓他公開過從 48218 連下來（7/23）。"
            "有畫才知道被破壞——沒附圖就只准講方法，不准發明連哪兩根。"
        ),
    },
    {
        "id": "degree2",
        "keys": re.compile(r"(位階二|波浪位階二|還是在波浪)"),
        "q": "還在波浪位階二——怎麼判？",
        "a": (
            "跟 9/10「9/8 起 abc、當天 c 末端」、9/11「細微波走 5 段」、"
            "9/12「過 47578 才維持右肩」同一組。"
            "他沒把計數升成大 3；右肩還在整理。位階不講死：可能先當 A，同一晚可並存多標籤，用點數一驗再驗。"
            "過 47578 只是維持高有過前高，不是自動升浪。"
        ),
    },
    {
        "id": "0930",
        "keys": re.compile(r"(9:30|9：30|黑手|第二段)"),
        "q": "9:30 第二段黑手表態——是日盤還是夜盤？",
        "a": (
            "2025-04-24 已寫「今日 9:30 以後的夜盤可看密切觀察」。"
            "2026-09-11 20:52：市場黑手真正會表態是在 9:30 的第二段，通常也是夜盤指數波動最大的時候。"
            "這則掛在夜盤主文樓下，最像夜盤 21:30 那一根／第二段，不要當成日盤 09:30 開盤就講死。"
            "夜盤 15 分官方有柱也只報高低，不准假裝數過他說的第二段。"
        ),
    },
    {
        "id": "c2c3",
        "keys": re.compile(r"(C-2|C-3|C2轉C3|轉 C-3)"),
        "q": "C-2 轉 C-3 他怎麼判？",
        "a": (
            "他自己說出現 C-2 轉 C-3 一定會發文通知。9/11 主文是細微波修正 5 段＋下降軌破壞＝初步止訊號，"
            "還沒升成 C-3。9/5 前後文有 B 波 vs C-2 逃命波的判斷框架：輕易越過壓力反而要小心是 C-2。"
            "9/15：今天強彈反而小心逃命波 C-2，仍不是已確認、也還沒發文升成 C-3。"
            "9/15 22:58：若 C-2 轉 C-3 仍先調節，來回差約 4000、C-3 末端他估 43500 附近——仍不是已確認。"
            "沒看到他發文就不要替他升浪。"
        ),
    },
    {
        "id": "aux",
        "keys": re.compile(
            r"(有緣人|沒講出來|輔助判|多重比對|某金融商品|"
            r"波浪沒辦法|100%\s*確認|主音|除了波浪)"
        ),
        "q": "除了波浪主音，他還用什麼輔助確認低點？",
        "a": (
            "2026-07-31 原文：波浪理論沒辦法 100% 確認 7/29 39384 為 A 波低，"
            "台積電＋某金融商品的量價結構已經 100% 確認。那則他說不會刪、希望有緣人解讀。"
            "某金融商品沒點名，不准寫死。"
            "沒講完、可從 1709 交叉還原的輔助："
            "台積電量價（7/16 不公開量價選多標籤；7/17 量 100022 收＝低；2/6 假跌破 1740 站回；"
            "10/13 大盤沒爆月最大量、改用台積電 8/20 最大量當籌碼代理；7/8 輔助判別就是台積電支撐）；"
            "費半／那指先行（5/9 躍過下降壓再加上費半那指早就 14 重疊才敢說 4/9 低）；"
            "夜盤是否先過下降壓（沒過＝還可能擴延）；第一次碰到次級四＋下跌走 5，不是每一根大陰都抄；"
            "風向球／長線龍頭是否先修正完成。"
            "2026-09-15 夜：又用某商品量價說大盤要跌破 43500 非常難；成交金額失真。某商品仍沒點名。"
            "單講趨勢向上、初步止訊號、右肩有守、無法判斷 5 或 9＝還不到確認。"
            "7/16 不破 40000、7/24 今天最低後來都錯；7/28 說台積電已跌完，真正低 7/29。不是買訊。"
        ),
    },
    {
        "id": "witness",
        "keys": re.compile(r"(四路|對質|現在確認|確認了沒|輔助現在|證人|疊得上)"),
        "q": "現在確認了沒？四路對質怎麼看？",
        "a": (
            "新角度：不把波浪當唯一證人。確認句要拿官方加權、台積電量價、費半、台指期日／夜對質。"
            "這次抓到加權／台積電各 486 根、費半 1255、台指期 397 日（夜盤 376）。"
            "確認句後 20 根加權沒再破低 77.8%（n=9）；確認且輔助≥2 為 83.3%（n=6）；"
            "初步／右肩觀盤只有 52.9%（n=85）——觀盤句不能當確認。"
            "最新官方日若輔助沒疊滿，推論還不到確認。46506 是台指期日盤低，不是加權。"
            "某金融商品沒點名，不准寫死。夜盤 15 分有官方柱也只報高低，不數段。不是買訊。"
        ),
    },
    {
        "id": "no-broker-tape",
        "keys": re.compile(r"(券商分點|籌碼流向|天天.*分點|分點.*進出)"),
        "q": "要天天看券商分點嗎？",
        "a": (
            "社團內化，不引用原文。"
            "他認為天天盯分點做不出績效；個股先看量價，不是看分點流向。"
            "話筒查股的主力成本是官方分點表底，那是另一張卡，不是他的進出法。"
        ),
    },
]


def named_stocks(text: str, tags: Optional[Sequence[Any]] = None, *, limit: int = 8) -> List[str]:
    found: List[str] = []
    for t in tags or []:
        s = str(t or "").strip()
        if s == "ABF":
            continue
        if s in _NAME_SID and s not in found:
            found.append(s)
    blob = text or ""
    for m in _NAME_RE.finditer(blob):
        s = m.group(0)
        # ABF／ABF三雄都是族群對照，不是點名南電 8046。
        if s == "ABF":
            continue
        if s not in found:
            found.append(s)
        if len(found) >= limit:
            break
    return found[:limit]


def _scope(text: str, stocks: Sequence[str], levels: Sequence[Any]) -> str:
    blob = text or ""
    has_idx = bool(_IDX.search(blob) or levels)
    has_stk = bool(stocks) or bool(_STOCKISH.search(blob))
    if has_idx and has_stk:
        return "mix"
    if has_idx:
        return "index"
    if has_stk:
        return "stock"
    return "talk"


def _family(text: str) -> str:
    blob = text or ""
    bits: List[str] = []
    if _INDUSTRY.search(blob):
        bits.append("industry")
    if _WAVE.search(blob):
        bits.append("wave")
    if _VOLUME.search(blob):
        bits.append("volume")
    if _TRACK.search(blob):
        bits.append("track")
    return "+".join(bits) or "other"


class _Ohlc:
    def __init__(self, db_path: str = "") -> None:
        self.twii: List[Tuple[str, float, float, float]] = []
        self.tx: List[Tuple[str, str, float, float, float]] = []
        self.stock: Dict[str, List[Tuple[str, float, float, float]]] = {}
        self._twii_lv: Dict[int, List[Tuple[str, str, float]]] = {}
        self._tx_lv: Dict[int, List[Tuple[str, str, str, float]]] = {}
        self._st_lv: Dict[str, Dict[int, List[Tuple[str, str, float]]]] = {}
        if db_path and os.path.isfile(db_path):
            self._load(db_path)

    def _load(self, db_path: str) -> None:
        conn = sqlite3.connect(db_path, timeout=30.0)
        try:
            for date, high, low, close in conn.execute(
                "SELECT date, high, low, close FROM index_daily WHERE symbol='TWII' "
                "AND high IS NOT NULL AND low IS NOT NULL"
            ):
                try:
                    h, l, c = float(high), float(low), float(close)
                except (TypeError, ValueError):
                    continue
                if h - l > 3500:
                    continue
                ymd = _ymd(date)
                if not ymd:
                    continue
                self.twii.append((ymd, h, l, c))
                for role, val in (("高", h), ("低", l), ("收", c)):
                    self._twii_lv.setdefault(int(round(val)), []).append((ymd, role, val))
            try:
                for date, session, high, low, close in conn.execute(
                    "SELECT date, session, high, low, close FROM futures_daily "
                    "WHERE high IS NOT NULL AND low IS NOT NULL"
                ):
                    try:
                        h, l, c = float(high), float(low), float(close)
                    except (TypeError, ValueError):
                        continue
                    ymd = _ymd(date)
                    if not ymd:
                        continue
                    sess = str(session or "regular")
                    self.tx.append((ymd, sess, h, l, c))
                    for role, val in (("高", h), ("低", l), ("收", c)):
                        self._tx_lv.setdefault(int(round(val)), []).append(
                            (ymd, sess, role, val)
                        )
            except sqlite3.Error:
                pass
            sids = sorted(set(_NAME_SID.values()))
            qmarks = ",".join("?" * len(sids))
            try:
                rows = conn.execute(
                    f"SELECT stock_id, date, high, low, close FROM daily_quotes "
                    f"WHERE stock_id IN ({qmarks}) AND high IS NOT NULL AND low IS NOT NULL",
                    sids,
                )
            except sqlite3.Error:
                rows = []
            for sid, date, high, low, close in rows:
                try:
                    h, l, c = float(high), float(low), float(close)
                except (TypeError, ValueError):
                    continue
                ymd = _ymd(date)
                if not ymd:
                    continue
                sid_s = str(sid)
                self.stock.setdefault(sid_s, []).append((ymd, h, l, c))
                bucket = self._st_lv.setdefault(sid_s, {})
                for role, val in (("高", h), ("低", l), ("收", c)):
                    bucket.setdefault(int(round(val)), []).append((ymd, role, val))
        finally:
            conn.close()

    def match_index(self, level: float, post_ymd: str, *, prefer: str = "") -> str:
        key = int(round(float(level)))
        post = _ymd(post_ymd)
        cands: List[Tuple[int, str]] = []
        want_tx = prefer in {"夜盤", "台指"}
        want_tw = prefer in {"加權", "大盤", "點位", ""}
        if want_tx or not want_tw:
            for delta in (0, 1, -1):
                for ymd, sess, role, val in self._tx_lv.get(key + delta, []):
                    dist = abs(int(ymd) - int(post or ymd))
                    label = "夜盤" if sess == "night" else "日盤"
                    cands.append(
                        (dist, f"台指期{label} { _dash(ymd) } {role} {_px(val)}")
                    )
        if want_tw or not cands:
            for delta in (0, 1, -1):
                for ymd, role, val in self._twii_lv.get(key + delta, []):
                    dist = abs(int(ymd) - int(post or ymd))
                    cands.append((dist, f"加權 {_dash(ymd)} {role} {_px(val)}"))
        if not cands:
            return ""
        cands.sort(key=lambda x: x[0])
        best_dist, best = cands[0]
        if best_dist > 40000:
            return ""
        return best

    def match_stock(self, sid: str, level: float, post_ymd: str) -> str:
        bucket = self._st_lv.get(str(sid) or "")
        if not bucket:
            return ""
        key = int(round(float(level)))
        post = _ymd(post_ymd)
        cands: List[Tuple[int, str]] = []
        for delta in (0, 1, -1):
            for ymd, role, val in bucket.get(key + delta, []):
                dist = abs(int(ymd) - int(post or ymd))
                if dist > 14:
                    continue
                cands.append((dist, f"{sid} {_dash(ymd)} {role} {_px(val)}"))
        if not cands:
            return ""
        cands.sort(key=lambda x: x[0])
        return cands[0][1]


def _iter_rows(db_path: Optional[str] = None) -> List[Dict[str, Any]]:
    from biaoke_archive import load_bundled_club
    from biaoke_desk import load_corpus

    out: List[Dict[str, Any]] = []
    seen = set()
    blob = load_corpus(db_path if db_path else None)
    for p in blob.get("posts") or []:
        row = dict(p)
        row["club"] = False
        aid = str(row.get("id") or "")
        if not aid:
            continue
        seen.add(aid)
        out.append(row)
    club = load_bundled_club() or {}
    for p in club.get("posts") or []:
        row = dict(p)
        row["club"] = True
        aid = "club:" + str(row.get("id") or "")
        row["id"] = str(row.get("id") or "")
        if not row["id"] or aid in seen:
            continue
        seen.add(aid)
        out.append(row)
    out.sort(
        key=lambda p: (
            str(p.get("date") or ""),
            str(p.get("time") or ""),
            0 if p.get("kind") != "reply" else 1,
            str(p.get("id") or ""),
        )
    )
    return out


def _level_hits(text: str, date: str, ohlc: _Ohlc) -> List[str]:
    from biaoke_walk import extract_index_levels

    hits: List[str] = []
    for item in extract_index_levels(text):
        level = item.get("level")
        role = str(item.get("role") or "")
        try:
            n = float(level)
        except (TypeError, ValueError):
            continue
        note = ohlc.match_index(n, date, prefer=role)
        if note:
            hits.append(f"{_px(n)}→{note}")
        else:
            hits.append(f"{_px(n)}→庫內對不到官方高／低，不編")
    return hits[:6]


def _stock_level_hits(
    text: str, date: str, stocks: Sequence[str], ohlc: _Ohlc
) -> List[str]:
    blob = text or ""
    if not stocks:
        return []
    out: List[str] = []
    seen = set()
    for m in _NUM.finditer(blob):
        raw = m.group(1)
        whole = raw.split(".")[0]
        if _YEAR.fullmatch(whole) and "." not in raw:
            continue
        try:
            n = float(raw)
        except ValueError:
            continue
        if n >= 15000 or n < 5:
            continue
        ctx = blob[max(0, m.start() - 24) : m.end() + 12]
        sid = ""
        for name in stocks:
            if name in ctx:
                sid = _NAME_SID.get(name) or ""
                break
        if not sid and len(stocks) == 1:
            sid = _NAME_SID.get(stocks[0]) or ""
        if not sid:
            continue
        key = (sid, round(n, 2))
        if key in seen:
            continue
        seen.add(key)
        note = ohlc.match_stock(sid, n, date)
        if note:
            out.append(f"{_px(n)}→{note}")
        if len(out) >= 4:
            break
    return out


def _qa_for(
    row: Dict[str, Any],
    ohlc: _Ohlc,
    prev_fifth: Optional[Dict[str, Any]],
    db_path: str = "",
) -> Dict[str, Any]:
    raw = str(row.get("text") or "")
    date = str(row.get("date") or "")
    kind = "樓下" if row.get("kind") == "reply" else "主文"
    club = bool(row.get("club"))
    cite = ""
    text = raw
    if kind == "樓下":
        try:
            from biaoke_ingest import split_author_cite

            cite, spoken = split_author_cite(raw)
            text = spoken or raw
        except Exception:
            cite, text = "", raw
    stocks = named_stocks(text, row.get("tags"))
    levels = _level_hits(text, date, ohlc)
    stock_lv = _stock_level_hits(text, date, stocks, ohlc)
    scope = _scope(text, stocks, levels)
    family = _family(text)
    q = f"{date} {kind}這則在判什麼？點位從哪來？"
    bits: List[str] = []
    if club:
        bits.append("社團內化，不引用原文。")
    if cite:
        bits.append("引號裡是路人的話，後面才是他自己回答。")
    if scope == "index":
        bits.append("這則在判大盤／台指／夜盤。")
    elif scope == "stock":
        bits.append("這則在判個股／族群。")
    elif scope == "mix":
        bits.append("這則大盤與個股一起判。")
    else:
        bits.append("這則是方法或補充。")
    if family == "industry":
        bits.append("主軸是產業趨勢／龍頭，不是把波浪套個股。")
    elif family == "wave":
        bits.append("主軸是大盤波浪／細微波。")
    elif "industry" in family and "wave" in family:
        bits.append("產業趨勢與大盤波浪並用。")
    elif "volume" in family:
        bits.append("主軸是量價／洗盤。")
    elif "track" in family:
        bits.append("主軸是升／降軌道連點。")
    if stocks:
        bits.append("點名 " + "、".join(stocks[:6]) + "。")
    for h in levels:
        bits.append("點位 " + h + "。")
    for h in stock_lv:
        bits.append("個股價 " + h + "。")
    if _FIFTEEN.search(text):
        night = bool(re.search(r"夜盤", text))
        use_tx = night or bool(re.search(r"台指", text))
        if db_path:
            try:
                from kline_hop import (
                    minute_cover_note,
                    minute_day_cover,
                    minute_night_cover,
                )

                cover = (
                    minute_night_cover(date, db_path)
                    if night
                    else minute_day_cover(
                        "TX" if use_tx else "TWII", "15", date, db_path
                    )
                )
                bits.append(minute_cover_note(cover, night=night))
            except Exception:
                bits.append(
                    "夜盤 15 分官方還沒進這段，不數這則的段。"
                    if night
                    else "庫沒 15 分，不數這則的段。"
                )
        else:
            bits.append(
                "夜盤 15 分官方還沒進這段，不數這則的段。"
                if night
                else "庫沒 15 分，不數這則的段。"
            )
    if ("勤誠" in text or "8210" in text or "勤誠" in stocks) and (
        _SIXTY.search(text) or "背離" in text
    ):
        if db_path:
            try:
                from kline_hop import minute_cover_note, minute_day_cover

                cover = minute_day_cover("8210", "60", date, db_path)
                if cover:
                    bits.append(minute_cover_note(cover, night=False))
            except Exception:
                pass
    if _TRACK.search(text):
        bits.append("軌道＝同一次級兩個更低的高（降）或浪2低連浪4低（升），不是均線。沒附圖就不發明連哪兩根。")
    if club and re.search(r"量先價行|爆.*大量", text):
        bits.append("介入先看量先價行：爆大量日高當壓、低當撐，價穩量縮才進。")
    if club and "停損" in text:
        bits.append("社團有點名停損價，話筒不貼；公開能講停損大約 7～10%。")
    if club and re.search(r"券商分點|籌碼流向", text):
        bits.append("個股先看量價，不是天天盯分點流向。")
    if _FIFTH.search(text) and prev_fifth is not None and _RETRACT.search(text):
        bits.append(f"相對 {prev_fifth.get('date')} 那則第五波規劃，這則是改口。")
    a = "".join(bits)
    if len(a) > 420:
        a = a[:419] + "…"
    return {
        "id": str(row.get("id") or ""),
        "date": date,
        "time": str(row.get("time") or ""),
        "kind": "reply" if row.get("kind") == "reply" else "post",
        "club": club,
        "q": q,
        "a": a,
        "scope": scope,
        "family": family,
        "stocks": stocks,
        "levels": levels[:6],
    }


def build_why_index(db_path: Optional[str] = None) -> Dict[str, Any]:
    if not db_path:
        try:
            from config import get_db_path

            cand = get_db_path()
            db_path = cand if cand and os.path.isfile(cand) else ""
        except Exception:
            db_path = ""
    ohlc = _Ohlc(db_path or "")
    rows = _iter_rows(db_path or None)
    cards: List[Dict[str, Any]] = []
    prev_fifth: Optional[Dict[str, Any]] = None
    for row in rows:
        card = _qa_for(row, ohlc, prev_fifth, db_path or "")
        cards.append(card)
        if _FIFTH.search(str(row.get("text") or "")):
            prev_fifth = row
    topics = [
        {"id": t["id"], "q": t["q"], "a": t["a"]} for t in _TOPICS
    ]
    n_pub = sum(1 for c in cards if not c.get("club") and c.get("kind") != "reply")
    n_club = sum(1 for c in cards if c.get("club") and c.get("kind") != "reply")
    n_rep = sum(1 for c in cards if c.get("kind") == "reply")
    return {
        "source": "1709+club+catchup-why",
        "n": n_pub,
        "n_club": n_club,
        "n_replies": n_rep,
        "n_cards": len(cards),
        "topics": topics,
        "cards": cards,
    }


def save_why_index(blob: Optional[Dict[str, Any]] = None, path: str = "") -> str:
    dest = path or WHY_GZ
    parent = os.path.dirname(dest)
    if parent:
        os.makedirs(parent, exist_ok=True)
    payload = blob or build_why_index()
    tmp = dest + ".tmp"
    with gzip.open(tmp, "wt", encoding="utf-8") as fh:
        json.dump(payload, fh, ensure_ascii=False, separators=(",", ":"))
    os.replace(tmp, dest)
    load_why.cache_clear()
    return dest


@lru_cache(maxsize=1)
def load_why() -> Dict[str, Any]:
    if os.path.isfile(WHY_GZ):
        with gzip.open(WHY_GZ, "rt", encoding="utf-8") as fh:
            blob = json.load(fh) or {}
        if int(blob.get("n_cards") or 0) >= 1700:
            return _merge_runtime_cards(blob)
    return _merge_runtime_cards(build_why_index())


def _merge_runtime_cards(blob: Dict[str, Any]) -> Dict[str, Any]:
    extra = list(_RUNTIME_CARDS)
    if not extra:
        return blob
    cards = list(blob.get("cards") or [])
    cmap = {str(c.get("id") or ""): i for i, c in enumerate(cards)}
    changed = False
    for c in extra:
        aid = str(c.get("id") or "")
        if not aid:
            continue
        if aid in cmap:
            cards[cmap[aid]] = c
        else:
            cards.append(c)
        changed = True
    if not changed:
        return blob
    out = dict(blob)
    out["cards"] = cards
    out["n_cards"] = len(cards)
    return out


def ingest_why_events(events: Sequence[Dict[str, Any]], db_path: str = "") -> int:
    """新抓到的主文／樓下立刻做成判斷卡，對官方 K 左證。"""
    global _RUNTIME_CARDS
    rows = [dict(r) for r in (events or []) if r.get("id") and r.get("text")]
    if not rows:
        load_why.cache_clear()
        return 0
    ohlc = _Ohlc(db_path or "")
    by_id = {str(c.get("id") or ""): c for c in _RUNTIME_CARDS}
    n = 0
    prev: Optional[Dict[str, Any]] = None
    for row in rows:
        card = _qa_for(row, ohlc, prev, db_path or "")
        by_id[str(card.get("id") or "")] = card
        n += 1
        if _FIFTH.search(str(row.get("text") or "")):
            prev = row
    _RUNTIME_CARDS = [c for c in by_id.values() if c.get("id")]
    load_why.cache_clear()
    return n


def refresh_minute_why_cards(db_path: str) -> int:
    """夜盤 15 分進庫後，重做有 15 分／夜盤原文的判斷卡。"""
    path = str(db_path or "").strip()
    if not path:
        return 0
    try:
        conn = sqlite3.connect(path, timeout=30.0)
        rows = conn.execute(
            """
            SELECT id, date, time, kind, parent, text FROM biaoke_posts
            WHERE text LIKE '%15分%' OR text LIKE '%15 分%' OR text LIKE '%夜盤%'
            """
        ).fetchall()
        conn.close()
    except Exception:
        return 0
    events = []
    for aid, day, hm, kind, parent, text in rows:
        events.append(
            {
                "id": aid,
                "date": day,
                "time": hm,
                "kind": kind or "post",
                "parent": parent or "",
                "text": text or "",
            }
        )
    if not events:
        return 0
    return ingest_why_events(events, path)


def latest_thread_digest(*, posts: int = 3, replies: int = 12) -> str:
    """最近三篇主文＋樓下自回。他幾乎不回更早的貼文。"""
    rows = _iter_rows()
    mains = [
        r
        for r in rows
        if r.get("kind") != "reply" and not r.get("club")
    ]
    if not mains:
        return ""
    latest = mains[-max(1, int(posts)) :]
    ids = {str(p.get("id") or "") for p in latest}
    under: Dict[str, List[Dict[str, Any]]] = {i: [] for i in ids}
    for r in rows:
        if r.get("kind") != "reply" or r.get("club"):
            continue
        pid = str(r.get("parent") or "").split(":")[0]
        if pid in under:
            under[pid].append(r)
    bits: List[str] = ["最近："]
    ordered = list(reversed(latest))
    for i, p in enumerate(ordered):
        pid = str(p.get("id") or "")
        clip_n = 800 if i == 0 else 180
        bits.append(
            f"{p.get('date') or ''} {p.get('time') or ''} 主文："
            + _clip(str(p.get("text") or ""), clip_n)
        )
        kids = under.get(pid) or []
        kids.sort(key=lambda x: (str(x.get("date") or ""), str(x.get("time") or "")))
        for r in kids[-max(1, int(replies)) :]:
            raw = str(r.get("text") or "")
            cite = ""
            spoken = raw
            try:
                from biaoke_ingest import split_author_cite

                cite, spoken = split_author_cite(raw)
                spoken = spoken or raw
            except Exception:
                cite, spoken = "", raw
            if cite:
                bits.append(
                    f"{r.get('date') or ''} {r.get('time') or ''} 樓下：引「"
                    + _clip(cite, 40)
                    + "」。他答："
                    + _clip(spoken, 120)
                )
            else:
                bits.append(
                    f"{r.get('date') or ''} {r.get('time') or ''} 樓下："
                    + _clip(raw, 140)
                )
    return " ".join(bits)


def is_why_query(ask: str) -> bool:
    q = (ask or "").strip()
    if not q:
        return False
    if _ALIEN.search(q):
        return True
    if _WHY_ASK.search(q) or _TALK.search(q):
        return True
    if named_stocks(q):
        return True
    if re.search(r"\d{4,5}", q):
        return True
    return False


def _topic_hits(ask: str) -> List[Dict[str, str]]:
    q = ask or ""
    out: List[Dict[str, str]] = []
    for t in _TOPICS:
        if t["keys"].search(q):
            out.append({"id": t["id"], "q": t["q"], "a": t["a"]})
    return out


def _card_hits(ask: str, blob: Dict[str, Any], *, limit: int = 3) -> List[Dict[str, Any]]:
    q = ask or ""
    tokens = [t for t in re.findall(r"[\u4e00-\u9fff]{2,}|[A-Za-z0-9.]{3,}", q) if t not in {"怎麼", "如何", "為什麼", "為何", "什麼"}]
    stocks = named_stocks(q)
    scored: List[Tuple[int, Dict[str, Any]]] = []
    for c in blob.get("cards") or []:
        if c.get("club"):
            blob_s = " ".join(
                [
                    str(c.get("date") or ""),
                    str(c.get("q") or ""),
                    str(c.get("a") or ""),
                    " ".join(c.get("stocks") or []),
                ]
            )
        else:
            blob_s = " ".join(
                [
                    str(c.get("date") or ""),
                    str(c.get("q") or ""),
                    str(c.get("a") or ""),
                    " ".join(c.get("stocks") or []),
                    " ".join(c.get("levels") or []),
                ]
            )
        score = 0
        for t in tokens:
            if t and t in blob_s:
                score += 2 if t.isdigit() or len(t) >= 3 else 1
        for s in stocks:
            if s in (c.get("stocks") or []) or s in blob_s:
                score += 3
        if score:
            scored.append((score, c))
    scored.sort(key=lambda x: (x[0], str(x[1].get("date") or "")), reverse=True)
    out: List[Dict[str, Any]] = []
    seen = set()
    for _s, c in scored:
        aid = str(c.get("id") or "")
        if aid in seen:
            continue
        seen.add(aid)
        out.append(c)
        if len(out) >= limit:
            break
    return out


def lookup(ask: str, *, limit: int = 4) -> str:
    q = (ask or "").strip()
    if not q:
        return ""
    if _ALIEN.search(q) and not re.search(r"(46506|47578|台光電|聯亞|細微波|飆客)", q):
        return "這不是飆客本人的聲音，不拿來當他的判斷。"
    bits: List[str] = []
    try:
        from biaoke_wave import format_wave_eyes, format_wave_now, is_wave_question

        if is_wave_question(q):
            db = ""
            try:
                from config import get_db_path

                db = get_db_path() or ""
            except Exception:
                db = ""
            live = format_wave_now(db, n=760)
            if live:
                bits.append(live)
            bits.append(format_wave_eyes())
    except Exception:
        pass
    try:
        from biaoke_weave import weave_lookup

        woven = weave_lookup(q, limit=3)
        if woven:
            bits.extend(x for x in woven.split("\n") if x)
    except Exception:
        pass
    for t in _topic_hits(q):
        if t["a"] not in bits:
            bits.append(t["a"])
        if len(bits) >= 3:
            break
    blob = load_why()
    if len(bits) < 3:
        for c in _card_hits(q, blob, limit=limit):
            prefix = "社團內化：" if c.get("club") else f"{c.get('date') or ''} {c.get('kind') or ''}："
            line = _clip(prefix + str(c.get("a") or ""), 280)
            if line and line not in bits:
                bits.append(line)
            if len(bits) >= limit + 1:
                break
    if (len(bits) < 2 and (is_why_query(q) or _TALK.search(q) or named_stocks(q))) or re.search(
        r"(最近|晚上|那則|樓下|他在看)", q
    ):
        digest = latest_thread_digest()
        if digest and digest not in bits:
            bits.append(_clip(digest, 1600))
    return "\n".join(x for x in bits if x)


def format_why_html(ask: str) -> str:
    body = lookup(ask)
    if not body:
        return ""
    return html_escape(body)


def format_why_notes(ask: str, *, limit: int = 3) -> str:
    body = lookup(ask, limit=limit)
    if not body:
        return ""
    return "判斷鏈 " + _clip(body.replace("\n", "／"), 1400)


def why_counts() -> Dict[str, int]:
    blob = load_why()
    return {
        "n": int(blob.get("n") or 0),
        "n_club": int(blob.get("n_club") or 0),
        "n_replies": int(blob.get("n_replies") or 0),
        "n_cards": int(blob.get("n_cards") or 0),
        "n_topics": len(blob.get("topics") or []),
    }


if __name__ == "__main__":
    dest = save_why_index()
    c = why_counts()
    print(dest, c)
