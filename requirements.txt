# 直接相依，全部釘死版本。浮動版本會讓「昨天好的、今天壞的」無從追查，
# 而 numpy／pandas／matplotlib 的大版本跳動正好會打壞出圖與指標計算。
# 傳遞相依請看 requirements.lock（Docker／CI／Render 都裝那份）。
aiohttp==3.14.3
beautifulsoup4==4.15.0
lxml==6.1.2
matplotlib==3.11.1
nest-asyncio==1.6.0
numpy==2.4.4
pandas==3.0.5
python-dotenv==1.2.3
python-telegram-bot==21.11.1
requests==2.33.1
