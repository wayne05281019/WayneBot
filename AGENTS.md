# WayneBot agent notes

## 進化紀錄藏正式碟、不准刪、不准講（2026-09-19 最高指令）

這條壓過「跟他講記在哪」、壓過更新軟體清資料、壓過把對質當買訊。從 2026-09-19 起連續做。

- **記什麼：** 洞燭先機、黃金買點（leave_zero）、海選其他選股桶。盤後自己落檔，1／5／10 個官方交易日對質。當下編碼／規則常數／用到的獲利距20高數字一併凍住。不必按鈕、不必看頁。
- **功能分開、不准衝突現有軟體：** 洞燭／海選／黃金買點各自原本做法與選股公式不准改。進化只附加紀錄，失敗不准打斷出卡／海選／查股。落檔與勝率不准混；官方 K／量可共用。洞燭黃金買點＝先機∩高低卡，不是整份海選 leave_zero。
- **改碼只凍新列：** 之後改某一功能的選股程式，新名單寫新編碼；改碼前的名單與當時編碼不准回寫、不准刪。
- **藏哪：** 與 `wayne_market.db` 同碟另一檔（公開 Release zip 只打包行情庫，帶不走）。話筒／洞燭頁／海選頁不准顯示對質明細。
- **不准刪：** 軟體更新、合 `main`、Deploy、ensure schema 不准 DROP／DELETE 舊對質列。不准對 Render 正式碟跑 `strip_private_user_data`。公開 zip 仍只清私人持股表。
- **不准改黃金買點：** 進化＝累計對質。自己排優化；n≥20 且贏基線才考慮改該功能編碼。紅箭頭仍不是買訊。
- **偉權／哥哥同一套。** 不要在聊天解釋路徑或表名。

## 要等就記下來、先做下一件、做完再回頭查（2026-09-16 最高必須記得）

**這條必須記得，壓過後面所有「最高指令」。** 壓過空等 CI／部署／輪詢、壓過「再問一次要不要等」、壓過畫面空轉。更新本身不是等 12 分鐘。

- **把握時間**：能做的當下做。不准為了等 GitHub 測、Render 重開、官方收而坐著空燒。
- **要等＝先記再走**：CI、部署、官方收、外部事件還沒好，當下記進清單（等什麼、哪一筆、回來怎麼核對），立刻做排程下一件。不准空等。
- **下一件做完＝回頭查**：那件可驗證收尾後，立刻回頭查等待項（CI 全綠就合 `main`、不准再問、對 `/health` `git_sha`）。好了就收；還沒好再記一次、繼續下一件。以此類推。
- **不准把等待當成進行中的工作**：等不是做事。沉默等、重複查同一跑、為了看起來忙而睡＝做錯。
- **一次一件仍在**：當下動手只做一件；被等擋住才切下一件，不是兩條功能同時進化。

## 時間一律台灣台北（2026-09-17 鎖死）

排程、抓文、彙整、建檔時戳、話筒說明，全部用 `Asia/Taipei`。美股欄位本身是美國時區的，標清楚來源，不准把台股排程改成 UTC。

## 該合就合、不用問（2026-09-17 最高指令）

全系統一樣。所有應合而未合的 PR 都走這條。壓過「再問一次要不要合」、壓過 Draft 停著等人點、壓過只合進分支。

- **該合＝CI 全綠、不是失敗／作廢的舊稿**：取消 draft，`gh pr merge --merge` 合進 `main`。不准再問。
- **衝突＝自己合 `origin/main` 解完再推**：CI 再綠就合。不准把 CONFLICTING 丟著。
- **不合的才不合**：CI 紅、已否決、明確失敗、過期 Draft 舊稿。那不是應合。
- **合進 `main` 才算更新**：對 `/health` 的 `git_sha`。偉權與哥哥同一顆 Bot。

## AI飆大＝條件更新＋等待可說、不准新價（2026-09-17 使用者鎖死）

這條壓過「演他講話」、壓過補下一帶價、壓過六顆各貼一句。目標是 AI 飆大，不是制式跟讀，也不是假飆大。

- **想：** 只在他自己點過的條件上做機率更新與改口。沒說過的價不准當下一步。
- **說：** 問到位階／他盯的位時，可以講「他還在等自己點過的哪個位；官方還沒收，所以不下判」。沒問到位不要硬套這句。
- **不准：** 新價、發明 5／9、把波浪套個股、盤中未收當官方、六顆各貼一句。
- **衝突就端上桌：** 這句跟先前鎖死打架、或跟代理人判斷不一樣，當下寫衝突／建議／請他選。沒選不准改大腦規則、不准默默選邊。
- **演算延伸：** 只畫他已點、官方還沒走到的位（如果句）。盤後要默默再試著畫並對質。不准補新價。話筒不准發明段號。
- **抽屜（2026-09-17 同鎖）：** 六顆（巢穴／產業／龍頭／量價／長抱／看錯）只當歸檔抽屜，關鍵字只負責丟進抽屜。開火只出一條判斷：上一句的位 → 官方柱碰到沒 → 改口或還在等。成長寫進 watch／tape／改口。五件只有疊在同一個他點過的位才叫交叉。
- **SYSTEM 只留硬規則（2026-09-17 同鎖）：** 提示詞只准守規則——抽屜、一條判斷、不准新價、未收不下判、個股不數浪、不是買訊。1709／自回是材料庫，當下去對，不准把日記背進 SYSTEM 演他。開口第一句也是這一條判斷，不是六顆交叉稿。

## 一次一件、不准空燒、五件串神經元（2026-09-16 最高指令）

這條壓過後面所有「再問一次」、壓過平行開第二件、壓過重複空轉。補充：不可以空燒模型。

- **永遠一次做一件事**：當下只准有一件進行中。同一件裡走最短路徑收到正確結果（測會破的檔、合 `main`、對 `/health` `git_sha` 算同一件收尾，不是另開一件）。不准同時進化兩條功能、不准邊改邊再開新主題。
- **被打斷＝先做完當下**：新指令進來，先把進行中的件做到可驗證的收尾（或寫清阻塞），才能接下一件。不准丟半途去追新話題。
- **最低耗能模型**：只准 Cursor Models；能少步就少步。永遠不能出錯＝省的是步驟與額度，不是正確性。要最大效率**且**最正確答案；看不懂、沒對質、蓋住數字＝沒做完。
- **不准空燒**：同一測、同一圖、同一輪詢、同一段消化，做過就停。為了看起來忙而多跑、多讀、多渲、再開子代理＝做錯。
- **可重複兩次，第三次必優化**：同一類動作（輪詢、官方收、tape、合主、對質、掃自回）出現第二次就要當重複；第三次不准再手做一遍，當下收成程式／清單／一次指令，簡化到之後自動或一行做完。優化不准把官方柱、自回、對質寫錯或漏掉。
- **未完成當下記錄**：被打斷、排好還沒做、做到一半，當下記進清單（已做完／做到一半／還沒做）。做完當下這件之後，不等使用者再說，回頭逐項做完。不准只留「之後再做」。
- **只要說到飆大**：推論只准走他的五件精神當脊骨去串神經元——波浪、形態、量價結構、關鍵K棒、碎形線。要疊在同一個他點過的位才叫交叉。六顆是抽屜不是嘴巴。不准發明 5／9、不准把波浪套個股。盤中未收不當官方收。

## 最省模型、結果不准打折（2026-09-15 最高指令）

永遠用最省錢的模型使用方式、最有效率的省錢路徑。這條壓過「再問一次」、壓過重複空轉、壓過多餘的圖／測／PR。

- **能一次做完就一次**：平行該平行的、只測會破的檔、只渲定稿。不准為了看起來忙而多跑。
- **結果與答案不准打折**：省的是步驟與額度，不是正確性、不是圖能不能看懂、不是驗證有沒有做。看不懂、蓋住數字、沒對質＝沒做完。
- **該做的動作仍要做**：推 `main`、建檔、對質、有需要再開 PR。省錢≠少做該做的。
- **重複動作立刻收成系統**：輪詢、官方收、tape、圖軸這類每次都要做的，寫進程式自動跑，不准每次人工重做一輪。優化／精簡不准讓官方柱、自回、對質寫錯或漏掉。

## 隨時看發文與全部自回，半途項列完做完（2026-09-15 最高指令）

他主文＋樓下**全部自回**（含回在很早留言、回文裡的回文、他去回別人）隨時都會改大盤位階、演算延伸、個股狀態。使用者每次查問，回覆都要吃到這些最新材料。路人樓下要收進討論串，正文不當他的判斷。

- **隨時看**：輪詢不只掃最新一篇主文；最近兩則討論串的一／二／三層自回、別人回他、他回別人每次都重讀。先入匣與輔助檔；台北 02:00 與開市日 13:00 才進神經元。交易日 08:00–09:00 每 10 分、09:01–13:30 每 3 分、13:30–15:00 每 10 分、其餘每 1 小時。
- **一件記一件**：主文、一層、二層、三層分開建檔，不准併成一句就丟。對官方 K、對質舊檔。
- **半途不准丟**：被打亂、排好還沒做、做到一半的，當下整理成清單，按件做完才算。不准裝在忙、不准只留下一句「之後再做」。

## 消化／實作時不准空燒（2026-09-15 最高指令）

想法消化、落成編碼時，這條壓過「再問一次要不要做」、壓過重複測同一批、壓過畫面空轉。

- **能省就自動省，正確性不准打折**：連續步驟、大量動作、可合併的指令，自己改成最短路徑（一次平行、一次測對的檔、一次渲定稿）。不准為了看起來忙而多跑、多渲、多讀。
- **同一件事只做一次**：已經合進 `main`、剛測過、剛渲過的，不准重做。發現自己在重複，立刻停、找原因、修根因，不要再空燒一輪。
- **沒有進行中的件＝立刻建議下一步**：不准等待放空、不准裝還在跑。建議要具體、一次一件、用他能回「做」的句。
- **卡住或怪（重複失敗、同一圖渲多次、同一測重跑）**：當下找出問題並解決，或寫出阻塞與下一步。沉默＝做錯。

## 飆大先存檔、兩個彙整窗才進神經元（2026-09-17 最高指令）

抓文節奏不變。分析／吸收不准跟每次輪詢綁在一起。

- **抓**：交易日 08:00–09:00 每 10 分、09:01–13:30 每 3 分、13:30–15:00 每 10 分；其餘每 1 小時。主文＋討論串一／二／三層自回。時間全部台北。
- **先存**：正文、圖檔 URL／抓得到的附件、點名個股的官方日 K（缺月現在補）、高低卡既有欄（20高／20低／60曆日收盤低／獲利％）、產業細項快取、五件標籤。彙整時不准再重找。
- **吸收**：只在台北每天 02:00，以及台股開市日 13:00，把匣裡的資料丟進神經元／watch／why／forecast。不是買訊。
- **仍立刻做**：官方 tape（他說的 vs 高低量）、緊急急跌推播。盤中未收不當官方收。IET＝IET-KY 4971。
- **大盤位階圖**：問位階要把他自己改口過的轉折連成線；不同區間／層級的線要拆開。沒對上的不准發明 1–2–3–4–5 段號。15／60 仍只服務大盤。
- **大盤默默練：** 盤後官方加權齊了，自己落紀錄＋演算延伸＋試畫每一段還沒走完的走法，官方柱走完對質對／偏／還沒走完，也對他的畫。要比電子夜盤／美指／美股時只讀已經進庫的官方欄，當下凍住給事後覆盤，不准現場重抓。台北 02:00 再覆盤檢討。不推話筒、不把練習段號寫進飆大回覆、不跟使用者報每日練習。現在話筒仍不准發明 5／9、不准套個股。
- **能講才講：** 大盤／洞燭／黃金買點都要這樣默默畫、紀錄、覆盤、檢討，自己比昨天強。不到能講的那天不准主動講。能講的那天才用「預計走勢會是…到哪個點有可能…建議現在提前…因為依照…觀察和分析」。黃金買點沒過關不准改公式、不准當新買訊。
- **不准**：發明 5／9、波浪套個股、六顆各貼一句、查股去打驗證碼頁。

## 話筒＝偉權／哥哥的 Telegram 軟體（2026-09-10 鎖死）

使用者講「話筒」，**只**指偉權與哥哥手機 App，或 Telegram 網頁版，裡面實際在用的那顆 `@WC_ai_trade_bot`。

- **不是** Cursor 話筒代理、computerUse、LINE Keep、本機模擬。
- **所有更新／建構只算上兩支手機**：把程式合進 `main`，等 Render `waynebot-service` 重開。只合進分支、只停在 Cursor、只本機測過＝沒做完。
- 核對有沒有更新：看 `https://waynebot-service.onrender.com/health` 的 `git_sha`，不要用正式 token 搶 `getUpdates`。
- 代理人禁止再開 Cursor 話筒代理去「轉傳／對圖」；那不是使用者說的話筒。
- 偉權與哥哥功能全同；鍵盤／飆大／查股都要兩邊同一顆程式。

## 產品憲法（2026-09-10 使用者鎖死）

**現在寫的、以後寫的，全部都要讓偉權與哥哥兩人都能完整用到每一個功能。** 這條壓過「先做偉權再補哥哥」、壓過單人測過就收工、壓過只合進分支不部署。

- **話筒就是兩支手機**：更新＝合進 `main` → Render `waynebot-service`。不是 Cursor 代理、不是 LINE Keep、不是本機話筒。
- **功能全同**：十二鈕、打代號兩張圖、海選／興櫃／連買／持股／觀察／AI倉／資金／大盤／記買入／飆大／洞燭先機，兩邊都能走完同一條路。不能做成哥哥精簡版、偉權專用鈕、或某條路徑只給一個 uid。
- **資料隔離不是功能隔離**：持股／觀察／AI倉／pending／上一檔／出圖檔按人分開，是避免洗掉對方，不是少給一邊功能。
- **「全部都看過」**：把這條對話多出來的東西再掃一遍，兩人 uid 都走一次，寫出已做完／做到一半／還沒做。想到就講的補充，不要被當下新話題把舊項丟掉。

## 最優先・不可違反（2026-09-07 使用者鎖死，看過 Spending 紅框）

Cursor 帳單「Included in Pro+」紅框裡有兩條：

- **上方 Cursor Models**（含 Cursor Grok、Composer）：**只准用這條。**
- **下方 Other Models**：**全面禁止。** 這條已經快用完（當時 95%）。不准再扣。

這條壓過後面所有規則、壓過「比較難」、壓過話筒／開 LINE／看片子／對圖／computerUse。違反＝做錯。

- 全部工作只准紅框**上方** Cursor Models。不准紅框**下方** Other Models。
- **禁止** Task／子代理／computerUse／Cursor 話筒代理。那些會吃 Other Models。卡住用本機渲圖、pytest、curl。使用者講「話筒」＝他們手機／網頁 Telegram，見文首。
- **禁止**指定 opus、sonnet、gpt、grok-high、grok-xhigh、claude 或其他 listed slug。`model` 只能省略或 `inherit`（必須仍是 Cursor Models）。
- 不能用換模型打斷 CI，也不能用正式 `TELEGRAM_BOT_TOKEN` 搶 `getUpdates`。

## 最高產品原則：哥哥功能全同、雙人同時重度使用（2026-09-08 使用者鎖死）

同一顆 `@WC_ai_trade_bot`。**所有修改／進化必須讓哥哥用得到的功能與偉權完全相同**，並且**以兩人同時重度使用為設計前提**。這條壓過「先做偉權再補哥哥」、壓過單人測過就收工。

- **功能全同**：十二鈕、打代號兩張圖、海選／興櫃／連買／持股／觀察／AI倉／資金／大盤／記買入／洞燭先機，兩邊都能走完同一條路。不能做成哥哥精簡版、偉權專用鈕、或某條路徑只給一個 uid。
- **資料隔離不是功能隔離**：持股／觀察／AI倉／pending／上一檔／出圖檔按人分開，是避免洗掉對方，不是少給一邊功能。
- **雙人同時重度使用**：兩人可以同時查同一檔或不同檔、同時走連買精靈、一邊海選一邊查股。pending、actor 鎖、出圖檔名、快取鍵必須按人分開；一人按「回主選單」不得清掉另一人的步驟。
- **不是雙 Bot**：已否決偉權／哥哥各一顆機器人。延後清單「雙 Bot」＝第二顆 token／第二套產品，沒規格。
- **版面**：偉權與哥哥都固定完整兩排（拿掉說明後往前、第二排最右洞燭先機）。沒有精簡鍵盤。洞燭先機＝佔比如實主判（CMoney 主產業／次產業／細項佔當日法人買超％升降），飆大找法只參考、不是唯一。輪動進哪一層要寫清，再分龍頭與次級（龍頭來不及買，比價下次級有黃金買點才切入）。不比張數、不發明一族。切入只認黃金買點。對五件只落在族群位階／形態／量價／官方K／第一名碎形，個股不數浪。

## 回掃節奏（2026-09-04 起，使用者指定）

每討論滿 **3 個主題**，或使用者連發 **5 段文字**，必須回頭掃一次先前討論，寫出：

- 已做完
- 做到一半
- 還沒做／漏看

想到就講的補充，自行判斷優先順序，不要被當下新話題把舊項丟掉。

## 只用 Cursor Models（2026-09-05 起；2026-09-07 對齊帳單紅框）

與文首「最優先」同一條：只准 Cursor Models，禁止 Other Models。下面是執行細節，不能放寬文首。

- Task／computerUse／Cursor 話筒代理預設不開；開了也會吃 Other Models。
- 禁止為了「比較難」、開 LINE、看片子去換模型。難關留在 Cursor Models 做。使用者要的「更新到話筒」＝部署 Render，不是開代理。

## 畫面要知道有沒有在做事（2026-09-04 起，使用者指定）

使用者看著畫面時，不能沉默。

- **沒在做事**：要寫出來「現在沒在做事」，不要讓畫面像還在跑。
- **有在做事**：一開頭就講正在做什麼、做到哪；等 CI／等外部事件也要講在等什麼。
- 這一回合收工時，寫清楚接下來是停還是下一件什麼。否則使用者會一直打斷來問進度。

## 禁止假資料／替代欄（硬規則）

沒有官方來源就不顯示、不寫庫、不算指標。沒有真數就不上卡，不要留空白欄。不准把虛構數字當成已知。

- 日 K：官方開高低收才准寫進 `daily_quotes`。`--`／`----`／收盤 0 不是價格，不准用昨收或最後買／賣價冒充今天收盤。庫裡 0 元柱與昨收複製列要刪。
- 禁止用均價 × 張數去推「外資／投信／融資／主力成本」。T86 只有股數沒有成本價。
- 禁止把三大法人叫主力。主力成本＝券商分點表底「平均買超成本」，不是外資＋投信＋自營。沒有真分點列就不要畫「主力成本」。
- 禁止畫「外資成本／投信成本／融資成本」。這三欄官方沒有成本價。
- 全市場分點日抓禁止（會打崩盤後日 K）。一次一件。查股不抓驗證碼頁。

## 排版測試（2026-09-11 起）

產品不再有「傳給 LINE」。改排版先本機把 Telegram 文字／高低卡渲成圖對過。

禁止用正式 `TELEGRAM_BOT_TOKEN` 跑 `getUpdates`（跟 Render 搶輪詢）。本機 `WAYNE_SKIP_POLLING=1`。
「更新到話筒」走 Render 正式服務，不要開 Cursor 代理去登他們的 Telegram。
CaryBot：要對高低卡準確性才開；對完關掉。不准當第二套策略來源。

## 已落地、不要再當待辦

- **Render 永久碟**：`wayne-data` 5GB，掛在 `/opt/render/project/src/data`（對到 `data/wayne_market.db`）。
- **話筒更新路徑**：合進 `main` → Render `waynebot-service`。核對 `/health` 的 `git_sha`。偉權與哥哥手機同一顆 Bot。
- **飆大 overlay**：公開文寫進同一顆 `wayne_market.db` 的 `biaoke_posts`，不進 `PRIVATE_USER_TABLES`，不進海選。**融合／匯入唯一底圖＝雲端硬碟那一千七百多則**（`archive_1709.json.gz`，`n >= 1700`），[Drive 資料夾](https://drive.google.com/drive/folders/1z4iNeBhO2-r1tlOLmv_vS-oNaAMaatXG)。**不准**用 `corpus_index.json` 520 篇當融合起點或寫回那檔。沒 1709 包就空，不要退回 520。開機 `seed_biaoke_archive`，盤中 ingest 也先補缺再 overlay 新文。只收飆大本人主文＋一／二／三層樓中樓（含回在別人留言裡的）＋他自己附的圖；路人留言收進討論串、不當他的判斷。抓文節奏不變。點名個股的輔助評斷資料與圖檔先存檔。神經元只在台北每天 02:00 與開市日 13:00 彙整吸收。社團 20 篇與社團 72 篇是雲端另外兩檔（跟 1709 不是同一批、20 也不是 72 的子集），只進 `archive_club.json.gz` 對價建檔／內化，**不進公開庫、不進話筒原文**。按飆大＝對話（無裡面選單、按鈕兩個字沒有圈），問句在 Render 上用這顆對話腦彙整回答；不准另開 Cursor 話筒代理、不准 Other Models。交易日 08:00–09:00 每 10 分、09:01–13:30 每 3 分、13:30–15:00 每 10 分抓最新主文＋最近兩則討論串；15:00 起到隔天 08:00、週末、國定假、颱風停市每 1 小時。盤中急跌或他出清／抄底／改口，自己研判後可跳過飆大鈕直接推偉權＋哥哥。樓下自回走同學會公開訪客 grant 打 Comments JSON；Render `CMONEY_AUTH_TOKEN` 只當備援（不要進 git、不要貼聊天）。
- **雙 Bot**：已否決。不是偉權／哥哥各一顆機器人；延後清單裡的「雙 Bot」是第二顆 token／第二套產品，沒規格。功能全同＋雙人同時重度使用見文首「最高產品原則」。
- **私人 Bot 白名單**：只認 Render 環境變數裡的兩個 Telegram uid（`TELEGRAM_CHAT_ID`＝偉權，`WAYNE_FAMILY_CHAT_IDS` 或逗號後＝哥哥）。真人 id 不准寫進 git。陌生人按開始回「這是私人 Bot」然後不理，也不進 `tg_users`。早報／AI 倉排程只寄白名單，不掃所有用過 Bot 的人。沒設任何 uid＝關門。
- **公開 Release zip**：只准日 K／財報這類公開行情。GHA 上傳前 `strip_private_user_data` 清持股／觀察／成交／AI 倉／`tg_users`。不要把 Render 正式碟整顆上傳。
- **Token 當密碼**：正式 `TELEGRAM_BOT_TOKEN` 只放 Render 環境變數。不准貼聊天、不准進 git、不准給 GHA 注入。GitHub 上那顆已 401＝作廢，BotFather 作廢那顆是人工一步。Cursor My Secrets 給新代理人寄訊，同樣不要貼出來。本機 `WAYNE_SKIP_POLLING=1`。
- **私人備份**：行情可用公開 zip 救災。持股／日記／AI 倉公開 zip 救不回。白名單帳號打「備份」會拿到自己那份 JSON；或本機 `scripts/backup_private_user_data.py`。拷到自己電腦或加密雲端，不要上傳 GitHub。

## 獲利驗收

獲利＝近 **60 曆日收盤低**。2383 永久驗收：收 5295／低 4100 → **29.1%**。
紅箭頭不是買訊；進場只認高低卡表。

## 高低卡為基底；密技不進程式（2026-09-05 起，使用者指定）

策略基底只認**原作者高低卡**。別人的密技、指標、沒公開公式的腳本，**不要寫進程式**，會打亂基底。

- **進場**：高低卡表。黃金買點一欄兩個標：買點（獲利剛離零，內部 `leave_zero`）才切入；還在零（60低超跌，內部 `golden_buy`）只觀察不是買。其他海選欄不是買訊。紅箭頭不是買訊。
- **出場協助**：只標作者公開「如何賣」（最高價＝20高 vs 最高溫）。查股／介紹圖／決策卡／持股／AI倉標減碼；黃金買點海選卡預告同一句（不同步就直接減碼）。不是買訊、不改海選桶、不自動賣。回測進場＋這句出場時，引擎閘與更寬樣本都要算，不准只留一層 n。
- **可收新規則的唯一條件**：能用 OHLC／量／均線／高低卡既有欄重述，且對流動母體採樣明顯較優。否則不接。
- **明確不要**：CMoney／TradingView 密技 1–57（沒公開公式不抄）、紫箭頭／CaryCross、沿 5／10 日不破就抱當出場、如何低買當買訊、策略2 當第二套「相對安全」標、#187 飆客稿（認錯語料，draft 不合）。
- Drive 再丟包：先讀、能量化再決定；不能量化就停在筆記，不要改海選或畫新箭頭。

## 目前未收斂（2026-09-07 再掃）

已合進 main、不要重做：決策卡時分秒（#162）、海選只收股票／KY、ETF 擋快取、作者公開說明當規則、主力成本不上卡、法說／股東會最近一件、虛擬倉留現金（#248）。

還開著：

1. **Telegram 真機**：規則不准用正式 token 搶 `getUpdates`。改排版先本機渲圖。
2. **飆客獨立區**：主選單第一排最右。語料可查、不進海選；能量化才考慮接到大盤。
3. **接券商真下單**：見下一節。AI倉先把訊號練準，**現在不准**把委託送進富邦。

## 之後才接券商（未做；2026-09-07 查過官方）

手機《富邦AI PRO》「量化積木」＝App 裡點選條件、回測、看訊號，**不能把 WayneBot 程式塞進去自動買賣**。

真下單要走富邦「新一代 API」（Fubon Neo API），不是去操控 App：

- 前置：開戶、電子交易憑證、簽署 API 使用聲明、先打測試環境。文件：https://www.fbs.com.tw/TradeAPI/docs/welcome/
- 語言：Python SDK 最合適（本專案就是 Python）。
- 進場指令形狀：`place_order(帳號, Order(symbol=代號, buy_sell=Buy, quantity=股數不是張, price=限價, price_type=Limit 或 Market, time_in_force=ROD))`。一張＝1000 股。
- 出場：同一支 `place_order` 改 Sell；或條件單／`TPSLWrapper` 停損停利（觸發後才送單，不保證成交）。
- App 積木條件（例如「1 分 K 五連紅」）對不齊高低卡，**不要抄進海選**。接 API 時只把已驗證的黃金買點表翻成委託。

未簽 API、未過測試環境以前：AI倉維持假錢對照組，永遠留第 3 份現金。
