# WayneBot agent notes

排程、抓文、彙整、建檔時戳、話筒說明，全部用 `Asia/Taipei`。美股欄位本身是美國時區的，標清楚來源，不准把台股排程改成 UTC。

下面 **1→12 的名次**才是最高指令。後面各節是做法與已落地細節，**不准再寫「壓過後面所有最高指令」去搶順位**。名次高的壓過名次低的。所有之後的指令、排程、動作都照這份走。

## 最高指令序（2026-09-19 鎖死）

1. 只用 Cursor Models
2. 最低耗能、最高效能、最正確答案
3. 能量化就直接量化（沒按也落檔；佐證用官方柱數字）
4. 不准假資料／買訊只認已量化回測過關的規則
5. 偉權哥哥功能全同（話筒＝兩支手機）
6. 排版：能一行就一行、單位對齊、圖文不互壓、不准孤字蓋數字
7. 功能該分開、不准跟現有軟體衝突；共用標籤一套，能同步的就統一換
8. 一次一件（畫面不能沉默）
9. 一次只問一項
10. 要等就記再走（本機先測）
11. 該合就合
12. 飆大規則

---

## 1. 只用 Cursor Models（2026-09-07 使用者鎖死，看過 Spending 紅框）

Cursor 帳單「Included in Pro+」紅框裡有兩條：

- **上方 Cursor Models**（含 Cursor Grok、Composer）：**只准用這條。**
- **下方 Other Models**：**全面禁止。** 這條已經快用完（當時 95%）。不准再扣。

違反＝做錯。壓過「比較難」、壓過開 LINE／看片子／對圖。

- 全部工作只准紅框**上方** Cursor Models。不准紅框**下方** Other Models。
- **禁止** Task／子代理／computerUse／Cursor 話筒代理。那些會吃 Other Models。卡住用本機渲圖、pytest、curl。
- **禁止**指定 opus、sonnet、gpt、grok-high、grok-xhigh、claude 或其他 listed slug。`model` 只能省略或 `inherit`（必須仍是 Cursor Models）。
- 不能用換模型打斷 CI。不准用正式 `TELEGRAM_BOT_TOKEN` 搶 `getUpdates`（跟 Render 搶輪詢）。本機 `WAYNE_SKIP_POLLING=1`。

## 2. 最低耗能、最高效能、最正確答案（2026-09-19 升成最高）

永遠使用最低耗能，創造最高效能及最正確答案。省的是步驟與額度，不是正確性。

- 能一次做完就一次。看不懂、蓋住數字、沒對質＝沒做完。
- 該做的動作仍要做：推 `main`、建檔、對質、有需要再開 PR。省錢≠少做該做的。
- 不准為了看起來忙而多跑、多讀、多渲、再開子代理。

## 3. 能量化就直接量化（2026-09-19 升成最高；2026-09-21 補默默落檔）

可以重複做的，**直接**收成可量、可排程、可對質。不必等做第三次。不准用假數去量化。

- 輪詢、官方收、tape、圖軸、合主、對質、掃自回：寫進程式自動跑，不准每次人工重做一輪。
- 策略新規則：能用 OHLC／量／均線／高低卡既有欄重述，且對流動母體採樣明顯較優才收。不能量化就停在筆記。
- 優化不准把官方柱、自回、對質寫錯或漏掉。

### 默默落檔（2026-09-21 鎖死）

**目標結果：** 每個鈕當天規則選出的名單都有檔，沒按也一樣。官方柱走完再對質。獨立交易日 n≥20 **且** 贏該功能現況，才考慮改那一條編碼。不到那天不准改黃金買點、不准改話筒畫面、不准主動講大盤預測、不准發明 5／9。大盤波浪圖是需求；能畫的那天再上話筒。優化進度自己持續記，對話裡不要講路徑或表名。

**每天留一套（沒按也留）：** 海選各桶、剛脫離零、當沖、隔日沖、連買、興櫃、洞燭、AI倉成交、大盤試畫。同一日同一規則只留一列，不准重複堆。持股／觀察是他們自己的本，不當「選股」。沒打代號就沒有查股名單。

**佐證同時留（數字，不是每檔截圖）：** 該日官方開高低收＋量。大盤再加加權完整柱、台指／電子日盤＋夜盤、美股已收（那指／費半／台積）。飆大：他自己文＋對得上的官方柱。缺完整官方收可以去同步補再凍；盤中未收仍不准當收。

**不留：** 每檔每天 K 圖 PNG／截圖（Render 碟 5GB，圖檔會滿）。未收盤柱。假成本欄。現場重抓來的未收數。

**會不會崩：** 名單＋官方柱數字一年大約幾十 MB，不會把伺服器或記憶體打爆。把每檔圖存成檔才會把碟吃滿、渲圖才會吃記憶體。所以對質用已存官方柱；圖要看時再從柱渲，不准為默默記去產圖。

**對話不准報：** 默默落檔、試畫、對質、還在收集，**不准**寫進「還沒做／做到一半」。使用者問進度、回掃、軟體完成了沒，都不要提這些。只有**明確優化狀態**才講一句：n 夠不夠、有沒有贏現況、要不要改那一條。沒有這句就當沒這件事。

## 4. 不准假資料／買訊只認已量化回測過關的規則（2026-09-19 改口）

沒有官方來源就不顯示。沒有真數就不上卡，不要留空白欄。

- 禁止用均價 × 張數去推「外資／投信／融資／主力成本」。T86 只有股數沒有成本價。
- 禁止把三大法人叫主力。主力成本＝券商分點表底「平均買超成本」。沒有真分點列就不要畫「主力成本」。
- 禁止畫「外資成本／投信成本／融資成本」。全市場分點日抓禁止。查股不抓驗證碼頁。
- **進場預設**只認高低卡黃金買點：買點（獲利剛離零，內部 `leave_zero`）才切入；還在零（60低超跌，內部 `golden_buy`）只觀察不是買。其他海選欄不是買訊。
- **紅箭頭可以當買訊，但必須先量化、也回測過關。** 現在話筒上的紅箭頭還沒過關，**現在仍不是買訊**。過關前不准改海選桶、不准當進場。
- 獲利＝近 **60 曆日收盤低**。2383 永久驗收：收 5295／低 4100 → **29.1%**。
- 出場協助：只標作者公開「如何賣」（最高價＝20高 vs 最高溫）。不是買訊、不自動賣。
- 密技不進程式。CaryBot：要對高低卡準確性才開；對完關掉。不准當第二套策略來源。

## 5. 偉權哥哥功能全同（話筒＝兩支手機）（2026-09-08／09-10 鎖死）

同一顆 `@WC_ai_trade_bot`。現在寫的、以後寫的，全部都要讓偉權與哥哥兩人都能完整用到每一個功能。以兩人同時重度使用為設計前提。

使用者講「話筒」，**只**指偉權與哥哥手機 App，或 Telegram 網頁版，裡面實際在用的那顆 Bot。不是 Cursor 話筒代理、computerUse、LINE Keep、本機模擬。

- **更新＝合進 `main` → Render `waynebot-service`。** 核對 `https://waynebot-service.onrender.com/health` 的 `git_sha`。只合進分支、只停在 Cursor、只本機測過＝沒做完。
- **功能全同**：十二鈕、打代號一次兩張圖、海選／興櫃／連買／持股／觀察／AI倉／資金／大盤／記買入／飆大／洞燭先機，兩邊都能走完同一條路。不能做成哥哥精簡版、偉權專用鈕、或某條路徑只給一個 uid。
- **資料隔離不是功能隔離**：持股／觀察／AI倉／pending／上一檔／出圖檔按人分開，是避免洗掉對方，不是少給一邊功能。
- **雙人同時重度使用**：pending、actor 鎖、出圖檔名、快取鍵必須按人分開；一人按「回主選單」不得清掉另一人的步驟。
- **不是雙 Bot**：已否決。延後清單「雙 Bot」＝第二顆 token／第二套產品，沒規格。
- **版面**：兩人都固定完整兩排。沒有精簡鍵盤。

## 6. 排版：能一行就一行、單位對齊、圖文不互壓、不准孤字蓋數字（2026-09-19 補進最高）

話筒是手機對話框，不是寬螢幕。排版錯、圖文互壓、字糊、數字被蓋住、看不懂＝沒做完。之後改文字／高低卡／介紹圖／產業圖／氣泡都照這份。

- **換行：** 整段文字能一行顯示就一行，真的排不下才跨行。不准為了好看提早斷行。氣泡大約 18 個全形字寬；長句在句號／頓號切開。
- **對齊：** 上下行的單位要齊（％、元、點、張、倍）。比價表、高低卡格、佔比列，同一欄上下對齊。標籤固定寬。
- **孤字：** 儘量不要孤字行，也不准行末只剩一個標點。不准把數字、％、股名從中間切開。
- **圖文不互壓：** 圖不能壓字、字不能壓圖、兩欄不能互壓。字不准疊字、不准壓線、不准蓋住數字。區塊之間空一行。高度跟內容走，不准為了塞進固定框把字壓扁。獲利％、高低價、佔比、買或不買，必須一眼能讀。
- **縮圖：** Telegram 會把圖縮成對話框寬。來源像素與字級要讓遠看兩張／單張縮圖也看得懂；點開維持高畫質。畫質低看到眼睛痛＝沒做完。
- **不重複：** 同一張資訊不准出現兩次（例如介紹圖下半導航與獨立導航圖）。能一次兩張就不要再堆四格；產業／導航留圖下鈕。
- **先本機對：** 改文字／高低卡／介紹圖／產業圖，先本機渲成圖對過再送 GitHub。不准用正式 `TELEGRAM_BOT_TOKEN` 搶 `getUpdates`。
- **兩邊同一張臉：** 偉權與哥哥看到的字級、間距、鍵盤、圖下鈕同一套。不能哥哥精簡到看不清。

## 7. 功能該分開、不准跟現有軟體衝突；共用標籤一套，能同步的就統一換（2026-09-19 升成最高）

洞燭、海選、黃金買點、大盤默默練、AI倉維持各自原本功能和做法，不准混成一套。進化不准騎亂現有程式。AI倉假錢對照組保留。

- **做法分開，資料一套：** 按鈕功能仍各做各的。產業／子項／資金鏈／資金流向這類共用標籤一旦修正，查股、決策卡、介紹圖、洞燭、海選、大盤，凡是會畫到這套資料的地方，要一次換成同一套。不准只改一個鈕、別的還留舊標。能同步替換的就統一更換。
- **各自落檔、各自對質、勝率不准混：** 大盤試畫／洞燭／海選／AI倉分開記、分開覆盤。官方 K／量可共用。進化只附加紀錄，失敗不准打斷出卡／海選／查股／問位階／AI倉。不准寫回黃金買點公式或洞燭名單。
- 勝率優化自己排；對話裡不要講路徑或表名。n≥20 且贏基線才考慮改該功能編碼。
- 沒有夠長、贏過現況的證據，**不准改黃金買點**。
- 洞燭先機＝佔比如實主判（CMoney 主產業／次產業／細項佔當日法人買超％升降），飆大找法只參考、不是唯一。輪動進哪一層要寫清，再分龍頭與次級。不比張數、不發明一族。切入只認黃金買點。

## 8. 一次一件（畫面不能沉默）（2026-09-16／09-04）

當下只准有一件進行中。不准同時進化兩條產品功能。同一件裡測／查可以平行。

- **被打斷＝先做完當下**：新指令進來，先把進行中的件做到可驗證收尾（或寫清阻塞），才能接下一件。測會破的檔、合 `main`、對 `/health` `git_sha` 算同一件收尾。
- **畫面不能沉默**：沒在做事要寫「現在沒在做事」。有在做要講正在做什麼、做到哪。等 CI／外部事件也要講在等什麼，但等不是進行中的工作。
- **未完成當下記錄**：被打斷、排好還沒做、做到一半，當下記進清單。做完當下這件之後回頭逐項做完。不准只留「之後再做」。默默落檔／試畫／對質不算使用者看得見的未完成，不准進這份清單。
- 能確定的（合主、排程、建檔）自己做。沒有進行中的件＝做下一件該做的，不准等待放空。

## 9. 一次只問一項（2026-09-19 升成最高）

要不要合、要不要等＝**不問**。規則打架、沒說過的價、會改買訊＝**問**。問的時候一次只丟一題，選完才改規則。沒選不准默默選邊。

## 10. 要等就記再走（本機先測）（2026-09-16／09-19）

不准為了等 GitHub 測、Render 重開、官方收而坐著空燒。更新本身不是等 12 分鐘。

- **先本機確認好再送 GitHub**。本機測過才推。GitHub 還沒好就去做下一件；綠了立刻合。
- 要等＝先記再走：當下記等什麼、哪一筆、回來怎麼核對，立刻做排程下一件。不准空等、不准重複查同一跑。
- 下一件做完＝回頭查等待項。好了就收；還沒好再記一次、繼續下一件。

## 11. 該合就合（2026-09-17）

全系統一樣。所有應合而未合的 PR 都走這條。不准再問要不要合。壓過 Draft 停著等人點、壓過只合進分支。

- **該合＝本機測過、CI 全綠、不是失敗／作廢的舊稿**：取消 draft，`gh pr merge --merge` 合進 `main`。
- **衝突＝自己合 `origin/main` 解完再推**：CI 再綠就合。不准把 CONFLICTING 丟著。
- **不合的才不合**：CI 紅、已否決、明確失敗、過期 Draft 舊稿。
- **合進 `main` 才算更新**：對 `/health` 的 `git_sha`。偉權與哥哥同一顆 Bot。

## 12. 飆大規則（2026-09-17／09-19）

目標是 AI 飆大，不是制式跟讀，也不是假飆大。不是買訊。

- **抓文隨時**：交易日 08:00–09:00 每 10 分、09:01–13:30 每 3 分、13:30–15:00 每 10 分；其餘每 1 小時。主文＋討論串一／二／三層自回。時間全部台北。
- **先存**：正文、圖檔、點名個股的官方日 K、高低卡既有欄、產業細項快取、五件標籤。彙整時不准再重找。
- **進大腦只準兩個窗**：台北每天 02:00，以及台股開市日 13:00。分析／吸收不准跟每次輪詢綁在一起。
- **仍立刻做**：官方 tape（他說的 vs 高低量）。盤中未收不當官方收。IET＝IET-KY 4971。
- **路人樓下**收進討論串，正文不當他的判斷。只認他自己主文和一／二／三層自回。
- **急跌**：會在抓文節奏與大盤頁讀加權對昨收。沒有為急跌去隨時盯加權的排程。不靠跌幅猜會到哪個浪級。單看加權跌幾點不推話筒。只有他新文自己写出清／到底，或官方碰到他已點過的位，才推兩人手機。不准補新價。
- **想／說**：只在他自己點過的條件上做機率更新與改口。沒說過的價不准當下一步。問到位階才講還在等哪個位。
- **波浪**：神經元對質到真的跟飆大一樣，才可以畫波浪。**現在還沒到，所以現在仍不准發明 5／9，也不准把波浪套個股。** 大盤他點過、官方柱對得上的轉折線可畫；沒對上的不准發明 1–2–3–4–5 段號。15／60 仍只服務大盤。
- **大盤默默練：** 盤後官方加權齊了，自己落紀錄＋試畫還沒走完的走法，官方柱走完對質對／偏／還沒走完。覆盤要的欄先凍：加權完整柱、他的標、試畫段、台指／電子日盤＋夜盤、美指／美股已收。缺完整官方收可以去同步補再凍；盤中未收仍不准當收、不准補假數。台北 02:00 再覆盤。不推話筒、不把練習段號寫進飆大回覆。不寫回查股／海選／黃金買點／洞燭名單／問位階／AI倉。失敗不准擋盤後融合。
- **能講才講：** 大盤／洞燭／黃金買點／AI倉各自默默畫、紀錄、覆盤。不到能講的那天不准主動講。能講的那天才用「預計走勢會是…到哪個點有可能…建議現在提前…因為依照…觀察和分析」。AI倉鈕留著。
- **五件**（波浪、形態、量價結構、關鍵K棒、碎形線）要疊在同一個他點過的位才叫交叉。六顆（巢穴／產業／龍頭／量價／長抱／看錯）只當歸檔抽屜，不是嘴巴。開火只出一條判斷：上一句的位 → 官方柱碰到沒 → 改口或還在等。
- **SYSTEM 只留硬規則**：抽屜、一條判斷、不准新價、未收不下判、現在個股不數浪、現在紅箭頭不是買訊。1709／自回是材料庫，不准把日記背進 SYSTEM 演他。
- 查股不打驗證碼頁。

---

## 回掃節奏（2026-09-04 起）

每討論滿 **3 個主題**，或使用者連發 **5 段文字**，必須回頭掃一次先前討論，寫出：已做完／做到一半／還沒做／漏看。想到就講的補充，不要被當下新話題把舊項丟掉。**默默落檔／試畫／對質／還在收集不准進這四欄**；沒有明確優化狀態就不要報。

## 排版測試（2026-09-11 起）

產品不再有「傳給 LINE」。改排版先本機把 Telegram 文字／高低卡渲成圖對過。更新到話筒走 Render 正式服務。

## 已落地、不要再當待辦

- **Render 永久碟**：`wayne-data` 5GB，掛在 `/opt/render/project/src/data`（對到 `data/wayne_market.db`）。
- **飆大 overlay**：公開文寫進同一顆 `wayne_market.db` 的 `biaoke_posts`，不進 `PRIVATE_USER_TABLES`，不進海選。**融合／匯入唯一底圖＝雲端硬碟那一千七百多則**（`archive_1709.json.gz`，`n >= 1700`），[Drive 資料夾](https://drive.google.com/drive/folders/1z4iNeBhO2-r1tlOLmv_vS-oNaAMaatXG)。**不准**用 `corpus_index.json` 520 篇當融合起點或寫回那檔。沒 1709 包就空，不要退回 520。開機 `seed_biaoke_archive`，盤中 ingest 也先補缺再 overlay 新文。社團 20 篇與社團 72 篇是雲端另外兩檔，只進 `archive_club.json.gz` 對價建檔／內化，**不進公開庫、不進話筒原文**。按飆大＝對話（無裡面選單、按鈕兩個字沒有圈）。樓下自回走同學會公開訪客 grant 打 Comments JSON；Render `CMONEY_AUTH_TOKEN` 只當備援（不要進 git、不要貼聊天）。
- **私人 Bot 白名單**：只認 Render 環境變數裡的兩個 Telegram uid（`TELEGRAM_CHAT_ID`＝偉權，`WAYNE_FAMILY_CHAT_IDS` 或逗號後＝哥哥）。真人 id 不准寫進 git。陌生人按開始回「這是私人 Bot」然後不理，也不進 `tg_users`。早報／AI 倉排程只寄白名單。沒設任何 uid＝關門。
- **公開 Release zip**：只准日 K／財報這類公開行情。GHA 上傳前 `strip_private_user_data` 清持股／觀察／成交／AI 倉／`tg_users`。不要把 Render 正式碟整顆上傳。
- **Token 當密碼**：正式 `TELEGRAM_BOT_TOKEN` 只放 Render 環境變數。不准貼聊天、不准進 git、不准給 GHA 注入。GitHub 上那顆已 401＝作廢。Cursor My Secrets 給新代理人寄訊，同樣不要貼出來。
- **私人備份**：行情可用公開 zip 救災。持股／日記／AI 倉公開 zip 救不回。白名單帳號打「備份」會拿到自己那份 JSON；或本機 `scripts/backup_private_user_data.py`。拷到自己電腦或加密雲端，不要上傳 GitHub。

## 高低卡為基底；密技不進程式（2026-09-05 起）

策略基底只認**原作者高低卡**。別人的密技、指標、沒公開公式的腳本，不要寫進程式。

- **明確不要**（除非日後量化且回測過關，另問）：CMoney／TradingView 密技 1–57、紫箭頭／CaryCross、沿 5／10 日不破就抱當出場、如何低買當買訊、策略2 當第二套「相對安全」標、#187 飆客稿（認錯語料）。
- Drive 再丟包：先讀、能量化再決定；不能量化就停在筆記，不要改海選或畫新箭頭。

## 目前未收斂（2026-09-07 再掃）

已合進 main、不要重做：決策卡時分秒（#162）、海選只收股票／KY、ETF 擋快取、作者公開說明當規則、主力成本不上卡、法說／股東會最近一件、虛擬倉留現金（#248）。查股一次兩張高畫質（介紹＋高低卡；產業／導航圖下鈕）以最新合進為準。

還開著：

1. **Telegram 真機**：規則不准用正式 token 搶 `getUpdates`。改排版先本機渲圖。
2. **飆客獨立區**：主選單第一排最右。語料可查、不進海選；能量化才考慮接到大盤。
3. **接券商真下單**：見下一節。AI倉先把訊號練準，**現在不准**把委託送進富邦。

## 之後才接券商（未做；2026-09-07 查過官方）

手機《富邦AI PRO》「量化積木」＝App 裡點選條件、回測、看訊號，**不能把 WayneBot 程式塞進去自動買賣**。

真下單要走富邦「新一代 API」（Fubon Neo API），不是去操控 App：

- 前置：開戶、電子交易憑證、簽署 API 使用聲明、先打測試環境。文件：https://www.fbs.com.tw/TradeAPI/docs/welcome/
- 語言：Python SDK 最合適（本專案就是 Python）。
- 進場指令形狀：`place_order(帳號, Order(symbol=代號, buy_sell=Buy, quantity=股數不是張, price=限價, price_type=Limit 或 Market, time_in_force=ROD))`。一張＝1000 股。
- 出場：同一支 `place_order` 改 Sell；或條件單／`TPSLWrapper` 停損停利（觸發後才送單，不保證成交）。
- App 積木條件對不齊高低卡，**不要抄進海選**。接 API 時只把已驗證的黃金買點表翻成委託。

未簽 API、未過測試環境以前：AI倉維持假錢對照組，永遠留第 3 份現金。
