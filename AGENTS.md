# WayneBot agent notes

## 回掃節奏（2026-09-04 起，使用者指定）

每討論滿 **3 個主題**，或使用者連發 **5 段文字**，必須回頭掃一次先前討論，寫出：

- 已做完
- 做到一半
- 還沒做／漏看

想到就講的補充，自行判斷優先順序，不要被當下新話題把舊項丟掉。

## 寫程式可換模型（2026-09-04 起，使用者指定）

寫困難程式時，可以自己改用更強的 listed 模型（Task 子代理指定 model slug），不必問「要不要換」。

換回去更重要：難關寫完、測過沒問題，立刻回到原本／較便宜的模型繼續收尾（文件、commit、PR、例行修測、回掃）。不要用高額度模型做簡單改動或空轉。

- 值得換：複雜推理、難除錯、大重構、規則對不齊。
- 不值得換：改字、補測、文件、開 PR、等 CI、日常小修。
- 子代理只能用工具描述裡列出的 model slug，沒列的不要猜。
- 換模型不能打斷已在跑的 CI，也不能用正式 `TELEGRAM_BOT_TOKEN` 搶 `getUpdates`。

## 畫面要知道有沒有在做事（2026-09-04 起，使用者指定）

使用者看著畫面時，不能沉默。

- **沒在做事**：要寫出來「現在沒在做事」，不要讓畫面像還在跑。
- **有在做事**：一開頭就講正在做什麼、做到哪；等 CI／等外部事件也要講在等什麼。
- 這一回合收工時，寫清楚接下來是停還是下一件什麼。否則使用者會一直打斷來問進度。

## 目前未收斂（2026-09-04 15:20 台北掃過）

1. **盤中決策卡右上角**：日期＋產出時分秒（程式已改，要 commit／PR）。
2. **今早海選要交名單**：起漲 2845 遠東銀、3703 欣陸、6177 達麗（基準日 9/3 收盤；9/4 官方 16:30 尚未融合）。
3. **查股等待**：進度要跟實際階段同步（介紹圖／決策卡／導航），不要只停在 0 秒。
4. **Telegram 真機對 Cary**：只打過 2383／2408／3008，14 檔還沒對完。
5. **作者公開說明**：當規則讀；不抄紅箭頭；舊文／盤中截圖不拿來對收盤。
6. **16:30 前庫沒今天**：即時列用 MIS／Yahoo，不要假裝已有官方收盤。
