{
  "name": "WayneBot",
  "install": "bash .cursor/install.sh",
  "terminals": [
    {
      "name": "waynebot-web",
      "command": "WAYNE_SKIP_POLLING=1 .venv/bin/python main.py",
      "description": "WayneBot web/health server on :10000 (/health, /inventory). Always send-only in Cursor Cloud: WAYNE_SKIP_POLLING=1 plus CURSOR_AGENT auto-skip, so TELEGRAM_BOT_TOKEN may sendMessage but never getUpdates against Render."
    }
  ],
  "ports": [
    {
      "name": "waynebot-web",
      "port": 10000
    }
  ]
}
